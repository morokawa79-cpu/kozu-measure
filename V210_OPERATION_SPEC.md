# 土地区画作成工房 v2.1 基本操作仕様

対象: `v2.1.0-alpha.5`

この文書は、旧フル版 `app.js` の操作を基準にし、JWW型UIへ載せ替える際にも変えてはいけない基本操作を定義する。

## 共通マウス操作

| 操作 | 仕様 |
|---|---|
| 左クリック | 作図点を追加する。既存の端点・交点・辺・有効なグリッドへ近い場合は自動吸着する |
| ダブルクリック | 区画、道路、折れ線、面積、通常線、分割線を確定する。ダブルクリック由来の重複終点は残さない |
| 右クリック | 作図中の最後の1点だけ戻す。確定済み図形や全体Undo履歴には触れない。作図点がなければ現在の段階を取り消す |
| 中ボタンドラッグ | 図面を移動する |
| Space＋左ドラッグ | 図面を移動する |
| ホイール | カーソル位置を中心に拡大・縮小する |

## キーボード

| キー | 仕様 |
|---|---|
| Enter | 条件を満たした作図を確定する |
| Backspace | 作図中の最後の1点だけ戻す |
| Esc | 未確定点を全消去 → 対象選択を解除 → 選択コマンドへ戻る、の段階取消 |
| Ctrl+Z / Ctrl+Y | 確定済み操作のUndo / Redo。文字入力欄では通常の文字編集Undo / Redoを優先する |

IME変換中のEnterは作図確定に使わない。入力欄のEscは入力欄を抜けるだけで、図面の作図状態を勝手に破棄しない。

## コマンドごとの確定方法

| コマンド | 確定方法 |
|---|---|
| 区画・道路・水路 | 3点以上。ダブルクリック、Enter、確定ボタン |
| 選択分割・一括分割 | 2点以上。ダブルクリック、Enter、確定ボタン |
| 折れ線距離・面積・通常線 | 2点以上（面積は3点以上）。ダブルクリック、Enter、確定ボタン |
| 2点距離・矢印 | 2点目で即確定 |
| 引出線 | 先端と文字位置の2点目で即確定 |
| 均等ガイド | 2点目で即確定し、同じコマンドを継続 |
| 合筆 | 隣接する2区画目を選択した時点で確定 |

確定後は原則として同じコマンドを維持し、連続作図できる。コマンド切替時は未確定点・対象・プレビューだけを破棄し、下絵、図面、ズーム、表示位置は維持する。

## PDF・画像下絵

- 下絵ランチャーの「開く」は1クリックでファイル選択を開く。
- ドラッグ＆ドロップ、クリップボード画像、差替、プロジェクト再読込を維持する。
- 初回読込でも表示倍率と表示位置を勝手に変更しない。
- 差替時は図形、縮尺、ズーム、表示位置を維持する。
- PDFは開いたページを先に表示し、全ページ文字解析を待たせない。
- 日本語CID/CAD PDF向けCMapと標準フォントをローカル資源から読む。
- 永続保存用Base64文字列と、画面描画用Canvasを混同しない。

## 回帰試験

- `npm run qa:v210`: 機能・保存・描画・レイアウトの総合試験。
- `node qa-v210-interaction.cjs`: 実ファイル入力、実マウス、実キーボードによる基本操作試験。

内部APIへ点を直接代入した試験だけで、クリック操作の合格とは判定しない。
