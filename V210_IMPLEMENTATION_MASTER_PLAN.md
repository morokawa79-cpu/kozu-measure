# 土地区画作成工房 v2.1 実装マスター計画

最終更新: 2026-07-18
対象: 土地区画作成工房 v2.1.0-alpha.10
目的: 会話が圧縮・分割されても、このファイルだけで設計判断と実装を再開できるようにする。

---

## 0. この文書の扱い

この文書を、今後の v2.1 実装における「再開地点」と「実装順の基準」にする。

判断の優先順位は次のとおり。

1. ユーザーがこの文書より後に明示した最新指示
2. この実装マスター計画
3. V210_FULL_FEATURE_SPEC.md
4. ui-usability-review-v210.md
5. V210_OPERATION_SPEC.md
6. 既存実装の挙動

既存コードに異なる挙動があっても、それだけを理由に仕様と見なさない。特に、無効なUI、英語の既定文字、縮尺の暗黙補正、図形座標を変更する出力処理は引き継がない。

この文書を更新するときは、完了・未完了・保留を明示する。会話だけで決めた内容を残さない。

---

## 1. 現在のリポジトリ状態

- リポジトリ: C:\Users\fmoro\マイドライブ\Claude\kozu-measure
- ブランチ: agent/kozu-v210-alpha8
- 実装開始時コミット: 66608d8 feat: v2.1.0-alpha.9の棚卸し改善を反映
- package version: 2.1.0-alpha.10
- 実行入口: main-v210.js
- 主な対象:
  - index-v210.html
  - jww-v210.css
  - v210/app.js
  - v210/core.js
  - v210/render.js
  - v210/io.js
  - main-v210.js
  - preload-v210.js
  - qa-v210.cjs

### 作業上の注意

- ワークツリーには、ユーザーまたは以前の作業による未コミット・未追跡ファイルがある。
- app.js、index.html、style.css、旧UI試作、builder設定などを無断で破棄・巻き戻し・削除しない。
- v2.1の実装は原則として v210 系ファイルを対象にする。
- V2_FEATURE_PLAN.md は既存の未追跡ファイルなので、この文書で上書きしない。
- 実装開始前に git status と差分を再確認し、今回の変更だけを分離する。

### 現在わかっている試験状態

- alpha.10の総合QAは299 / 299項目合格。
- 固定メニューバー、独自ポップアップの極小化、入力終了時の自動確定、区画・道路・水路の作図／選択対称性を最新仕様の期待値で検査済み。
- 保存・読込、原子的保存、schema v7の種類固有状態、分割後の全ID一意性を専用試験で確認済み。
- A4・A3の縦横PDF MediaBox、300dpi PNGのpHYs情報、出力時の用紙・縮尺計算を実成果物で確認済み。
- package、画面、main、preload、core、io、保存JSONの版番号をalpha.10へ統一。
- Windows x64インストーラーを作成し、87,679,116 bytes、SHA-256 `CAAEE3C1E77EB19ADB65846BB8A82ABAC4D8130EE6813AB90D4DDF774FAEE639`、展開版タイトル `土地区画作成工房 v2.1.0-alpha.10` を確認。
- GitHubへalpha.10コミット `c989efb` をプッシュし、Draft PR #1をalpha.10の内容へ更新。
- Notionへalpha.10開発ログを作成（ページID `3a1c75d6-89bc-819f-991a-d39d269b68ec`）。

---

## 2. 絶対に崩さない設計原則

### 2.1 実測・印刷・画面表示を分離する

次の3つを別の状態として扱う。

1. 下絵校正・mpp
   図面上の1座標が実世界で何mかを決める。面積・辺長・計測値の根拠。

2. 出力縮尺
   A3/A4の紙面上で、実世界の長さを何mmで印刷するかを決める。

3. 出力配置
   図形全体を用紙上のどこへ置くかを決める。図形個別の座標は変えない。

出力縮尺や用紙上の配置を変えても、図形頂点、文字位置、下絵位置、mpp、面積、寸法値は変えてはならない。

### 2.2 選択は1つ、編集画面は種類別

- 左側の「選択」機能は1つに保つ。
- 選択後に、対象種類ごとの専用編集画面へ切り替える。
- 作成時に設定できた項目は、原則として選択後も同じ意味で再編集できる。
- 違う種類に無効な入力欄は、無効表示で残すのではなく表示しない。
- 同種複数選択は、その種類の専用編集画面で一括編集する。
- 異種複数選択は、移動・複写・削除・表示など安全な共通操作だけを出す。

### 2.3 自動判断より確認可能性を優先する

- PDF記載縮尺の自動検出機能は削除する。候補提示にも使わない。
- よく使う縮尺候補をユーザーが選ぶ。
- スキャン、再保存、画像化、用紙サイズ変更が疑われるPDFは2点校正を使う。
- 複数ページPDFはページごとに縮尺・校正を設定する。
- 寸法依存操作は、そのページの縮尺が未設定なら明確に警告し、必要な場合は確定を止める。
- 出力時に図形がはみ出す、または小さすぎる場合も、自動で縮尺を変えない。候補と理由を示し、ユーザーが選ぶ。

### 2.4 Undoと保存を機能の一部として扱う

- 1操作は1回のUndoで戻る。
- 途中操作、右クリック、Esc、1点戻す、ポインタキャンセルを含めて状態を壊さない。
- 作成、編集、Undo、Redo、保存、再読込、出力まで揃って初めて機能完了とする。
- 保存前に編集中の入力値を必ず確定してから直列化する。

---

## 3. 全体の優先順位

### 最優先: データ消失と出力精度

1. 保存・開く・未保存確認を正常化
2. 下絵校正と出力縮尺を完全分離
3. ページ別の出力設定を保存
4. 図形座標を書き換える「用紙中央へ」処理を廃止
5. 出力プレビューで図形全体をマウス移動
6. 見た目の外接範囲から、はみ出し・小さすぎを判定
7. ユーザー選択式の出力縮尺候補
8. 画面・PNG・PDFで線幅を同じ物理寸法にする
9. PDF下絵の出力解像度、PNGの300dpi情報を修正
10. A4/A3、縦横、複数ページ、実寸の自動試験

### 次点: 選択UIの再編

11. 種類別編集画面の土台
12. フォント・文字配置モデルの統一
13. 区画・道路・水路・隅切り
14. 計測・寸法
15. 注記・スタンプ・表・補助線
16. 囲い選択・一括処理
17. 旧JSON移行と総合試験

出力系と保存系の基礎が壊れたまま、見た目だけのUI整理を先行しない。

---

## 4. 保存・読込・安全確認

### 現状認識

- 右側に保存はあるが、ユーザーから「効いていない」と報告がある。
- 保存直前に未確定の入力を反映しないため、直前の編集値が保存されない可能性がある。
- 現在ファイルパスを保持しないため、毎回「名前を付けて保存」相当になっている。
- 右側に作業ファイルの「開く」がなく、保存と対になっていない。
- Ctrl+Oは下絵を開く操作に使われている。

### 実装方針

- saveProject の直前に commitPendingEdit を必ず実行する。
- 初回保存は「名前を付けて保存」。
- 2回目以降の保存は、現在の作業ファイルへ上書きする。
- 「名前を付けて保存」は別操作として残す。
- DocumentStoreとは別に、現在の作業ファイルパスをruntimeで管理する。
- HTMLのfile inputだけに依存せず、main-v210.jsとpreload-v210.jsへ作業ファイル専用のnative open IPCを設け、選択した絶対パスをrendererへ返す。
- 終了時保存がDownloadsへ別名ファイルを作る現行経路は廃止し、現在パスへの保存または明示的な名前付き保存だけにする。
- 右レールに「開く」と「保存」を対で配置する。
- 作業ファイルを開くショートカットは Ctrl+Shift+O を候補にする。
- Ctrl+Oは下絵を開く操作のままでもよいが、表示名を明確にする。
- dirtyな状態で次を実行する前に、保存／破棄／取消を必ず確認する。
  - 新規作成
  - 新しい下絵を新規文書として開く
  - 別の作業ファイルを開く
  - アプリを閉じる
- 保存に失敗した場合、次の文書への移動や終了を行わない。
- 「下絵を差替」は図形を保持する操作として明確に分ける。
- 「白紙から開始」は実態に合わせて「下絵なしで作図」等へ改名する。

### 受入条件

- 入力欄に値を入れ、別欄をクリックせず保存しても、その値が再読込後に残る。
- 初回保存後、右の保存で同じファイルが更新される。
- 新規・下絵新規読込・作業ファイル読込・終了の全経路で未保存確認が出る。
- 取消を選ぶと文書・履歴・選択が一切変わらない。

---

## 5. 下絵・縮尺・ページ

### 方針

- PDF縮尺の自動検出は機能自体を削除する。
- 「PDF記載縮尺」という誤解を招く自動UIを残さない。
- 下絵UIを、次の順で分かるようにする。
  1. 下絵を開く／差替
  2. 現在ページ
  3. 縮尺設定方法を選ぶ
  4. 候補縮尺は選択時、任意縮尺は入力終了時、2点校正は条件が揃った時に自動保存
  5. 設定済み状態を表示
- 複数ページPDFは、各ページに独立した校正情報を持つ。
- ページを切り替えたとき、未設定ページには未設定を明示する。
- 未設定のまま距離・面積・隅切り・平行線等を開始した場合、モーダルを出さずページ・縮尺へ自動遷移し、固定バー内で必要性を説明する。

### よく使う図面縮尺候補

優先表示:

- 1:50
- 1:100
- 1:200
- 1:250
- 1:300
- 1:500
- 1:1000

建築図面向け:

- 1:5
- 1:10
- 1:20
- 1:25
- 1:30
- 1:75
- 1:150
- 1:400
- 1:600
- 任意入力

### A3 1:100でも実長が合わない理由

- 元PDFが真のA3で、図面内容が100%の1:100として保存されている場合だけ、用紙寸法から1:100を適用して正しくなる。
- スキャン、プリンタの「用紙に合わせる」、PDF再保存、画像化、余白追加、ページサイズ変更があると、記載が1:100でもデータ上の長さは1:100ではない。
- その場合は2点校正が必要であり、ソフトが「A3・1:100」という文字だけから正確な実長を保証してはいけない。

### UI改善

- 2点校正で2点目を押した時点で実距離が未入力なら、実距離入力欄へ自動フォーカスする。既に有効値があればその時点で保存する。
- 下絵読込後は「下絵を開く」を最優先色にしない。次に必要な縮尺設定を目立たせる。
- 縮尺未設定で区画を作った場合、「縮尺未設定のため寸法は表示されない」ことを案内する。

---

## 6. 選択システムと種類別編集画面

### 対象種類

形状:

- lot: 区画
- road: 道路
- water: 水路
- cutout: 隅切り

エンティティ:

- distance: 2点距離
- polyline: 折れ線距離
- area: 面積
- dimension: 寸法
- line: 線
- arrow: 矢印
- text: 文字
- callout: 引出線
- north: 北マーク
- house: 家屋
- parking: 駐車
- lot-table: 面積表
- guide: 均等・等分ガイド
- parallel: 平行線

### 種類別編集画面

| 種類 | 主な編集ページ |
|---|---|
| 区画 | 基本／面積・坪／辺寸法／表示／台帳 |
| 道路 | 道路情報／道路名／幅員／計測表示／表示／台帳 |
| 水路 | 水路情報／水路名／水路幅／計測表示／表示／台帳 |
| 隅切り | 隅切り情報／寸法／斜線／表示／元区画へ戻す |
| 2点距離 | 値・表示／線／文字／位置 |
| 折れ線距離 | 合計／区間値／線／文字／位置 |
| 面積 | ㎡・坪／辺寸法／塗り／文字／位置 |
| 寸法 | 値／補助線／矢印／文字。旧データ互換を優先 |
| 線 | 線種／色／太さ／端点 |
| 矢印 | 線／矢尻／文字／位置 |
| 文字 | 内容／書体／配置／色／背景・枠 |
| 引出線 | 内容／書体／線／先端／折点／位置 |
| 北マーク | 大きさ／角度／色／線幅 |
| 家屋 | 幅・奥行／大きさ／文字／塗り／線／斜線 |
| 駐車 | 幅・奥行／大きさ／文字／塗り／線 |
| 面積表 | 対象／列／書体／罫線／更新方法 |
| ガイド | 元の2点／分割数／表示／再作成 |
| 平行線 | 基準線／間隔／反転／本数／表示 |

### 単一・複数選択

- 単一選択と同種複数選択でタブ名と項目配置を統一する。
- 複数選択時は変更した項目だけを反映する。
- 異なる値は「混在」と表示し、勝手に先頭値へ揃えない。
- 異種複数選択では、種類固有の入力欄を出さない。
- 「選択→寸法」で別メニューになる既存構造を整理し、選択対象の専用画面内に寸法編集を統合する。
- 合計値と辺・区間個別値は分けるが、入口を乱立させない。

---

## 7. 区画・道路・水路・隅切り

### 7.1 種類切替

区画⇔道路⇔水路の変更は、単なるkindの書換えではなく構造変更として扱う。ただし、UIは種類selectを変更した時点で即時実行し、別の「種類を変更」「確定」ボタンや確認画面は置かない。

- 単一選択時のみ実行可能。
- 1回のUndoで元へ戻る。
- 頂点、形、共通メモは保持する。
- 種類固有の値をその場で永久削除しない。`kindStates.lot / road / water`のような退避領域へ保存し、同じ図形を元の種類へ戻した時は以前の番号、価格、名称、幅員、文字位置、辺書式を復元する。追加する場合はschema v7とv6→v7移行試験を行う。
- 区画から道路・水路:
  - 番号、価格、区画専用表示は画面と成果物から外すが、区画状態として内部保持する
  - 道路または水路の既定色を適用
  - 名称を日本語の「道路」または「水路」にする
  - 英語の road を絶対に出さない
- 道路・水路から区画:
  - 保存済みの区画状態があれば復元し、なければ区画番号を新規採番
  - 区画の既定色、面積、坪、表示初期値を適用
- 道路と水路へ切り替えた時、番号入力は画面から消す。
- 変換直後は「区画から道路へ変更しました。Ctrl+Zで戻せます」のように固定ステータスへ表示する。

### 7.2 道路・水路の文字

- 道路名と幅員は別の文字役割として保存・編集する。
- 道路名だけ変わり幅員が変わらない隠れた挙動をなくす。
- 「道路名と同じ書式にする」は明示的な1回操作として用意できる。
- 水路では「幅員」ではなく「水路幅」と表示する。
- 道路を縦書きにした場合、幅員も縦書きにするという既定連動を採用する。
- それ以外の書体・色・大きさの連動は、ユーザーが分かる形で操作する。

### 7.3 道路・水路の面積と寸法

道路と水路にも区画同様、必要に応じて次を表示できるようにする。

- 辺寸法
- 面積㎡
- 坪
- 約表記

初期値はすべて非表示。約表記は辺長、㎡、坪へ一貫して適用する。

道路・水路の作図中は各辺の仮寸法を表示し、確定直後は上記の初期値どおり非表示にする。ユーザーが選択後に表示をONにした場合だけ成果物へ出す。

### 7.4 色

- 道路の塗りにグレー系を追加する。
- 道路作成時と道路選択時で同じ色パレットを使う。
- 道路の外枠も同じ考え方で揃える。
- 選択画面では区画の共通色に加え、道路系の色を出してよい。
- 水路は水色系を中心にする。
- 区画の塗り色を少なくとも4色追加する。

### 7.5 分割・合筆・隅切り

- 現在の分割が区画限定なのは暫定仕様。最終仕様では区画・道路・水路を扱う。
- 選択分割:
  - 元の種類と道路・水路固有属性を両方へ継承
  - 名称、幅、色、文字設定、計測表示、メモを継承
  - ラベル位置は各新図形の中央へ再設定
  - 2つの子図形、全辺、全要素へ文書全体で一意なIDを割り当てる。元の外周辺の書式は幾何対応で引継ぎ、新しく生じた分割辺は新IDと既定書式にする
  - 1回のUndo
- 一括分割:
  - 対象種類をチェックボックスで明示
  - 区画は初期ON、道路・水路は初期OFF
  - 全対象を事前検証し、1件でも不正なら全体を変更しない
- 合筆:
  - 隅切り後の元区画と関係図形を正しく合筆・復元できる
  - 同種の区画+区画、道路+道路、水路+水路を基本とする
  - 道路名や幅等が異なる場合も確認画面は出さず、先に選んだ図形の属性を採用する。結果と採用元を固定ステータスへ表示し、1回のUndoで完全復元する
  - 価格、任意文字、外周辺の個別書式、ラベル位置を何から引継ぐかを属性ごとに定義し、未定義のままnullへしない
- 隅切り:
  - 現在の「底辺2m」という表現が、実処理と違うなら修正
  - 隣接する2辺上の距離で切ることをUIで明示
  - 元区画IDと親子関係を保存し、「元区画へ戻す」を可能にする

---

## 8. 文字・書体・数値入力

### 書体モデル

内部値を次に統一する。

- gothic: ゴシック
- mincho: 明朝
- mono: 等幅

既定は gothic。実CSS名 Yu Gothic UI 等を直接select値に入れない。既定がゴシックなのに選択欄が空白になる不具合をなくす。ユーザーが希望した「ゴシック／明朝／均等」という見せ方に合わせる場合、mono のUI表記は「均等（等幅）」とし、機能内容を誤解させない。

対象は、区画名、道路名、幅員、水路名、水路幅、面積、坪、辺寸法、計測値、文字、引出線、矢印文字、家屋、駐車、面積表を含む全文字。

注意: 等幅フォントと「均等割付」は別機能。文字を一定幅へ均等配置したい場合は、書体ではなく配置設定として別に用意する。既存の選択→寸法だけにある「均等」がどちらの意味かをコードで確認してから、全文字へ同じ意味で展開する。

### 紙面上の文字高・線幅

出力縮尺を変えても読みやすさを保つため、注記要素の見た目は紙面mmを基準にする。

- 文字高の標準候補: 1.8、2.5、3.5、5.0、7.0mm
- 線幅の標準候補: 0.13、0.18、0.25、0.35、0.50mm
- 新規の通常文字は2.5mm、通常線は0.25mmを初期候補とする。
- 文字高、線幅、矢尻、ハッチ間隔、寸法線と文字の間隔は紙面mmで保持する。
- 区画、道路、家屋等の実形状と配置点はworld座標で保持する。
- 旧px値は legacyPx × 25.4 ÷ 96 で紙面mmへ決定的に移行し、標準候補へ勝手に丸めない。
- UIでは標準候補に加えて任意値を許可する。
- 紙面固定文字があるため、収まり判定は縮尺候補ごとに文字外形を再計算する。

### 面積・坪の編集

- m²と坪の文字書式ポップアップで実際に効かない項目は削除する。
- 文字サイズは上部メニューバーで直接調整できるようにする。
- 面積入力は全角数字を半角へ正規化し、表示用に㎡を自動付与する。
- 坪入力も全角数字を半角へ正規化し、表示用に坪を自動付与する。
- 保存値は数値と単位を分離し、単位を重複保存しない。
- 自動計算値と手入力上書きを区別する。

---

## 9. 計測・寸法

### 作図中のライブ表示

次の作図中に、確定前から寸法を表示する。

- 2点距離
- 折れ線距離
- 面積
- 区画
- 道路
- 水路

折れ線は直近区間値と合計値、面積は辺長と仮面積を混同しない表示にする。

### 寸法文字の位置

- 2点距離と折れ線距離の寸法は線の上側を既定とする。
- 線の向きが逆でも、読める向きと視覚上の上側を保つ。
- 「戻す」「既定へ戻す」で線の下へ戻ってしまわない。
- 手動位置を設定した場合はそれを保存する。

### 編集入口

- 全体設定と、辺・区間ごとの個別設定を明確に分ける。
- 同じ寸法値を編集する重複欄を削除する。
- object-dimension-value 等の未使用・重複入力は棚卸しして除去する。
- 合計文字と区間文字の両方で、ゴシック・明朝・等幅を選べる。

---

## 10. 注記・スタンプ・表

### 文字・矢印・引出線

- 注記文字は、配置前の入力内容がクリック時にそのまま図面へ反映される。
- 選択後も内容、書体、色、大きさ、角度、縦書き、背景、枠を再編集できる。
- 文字内容を「台帳情報」の名称に隠さず、専用画面の先頭に出す。
- 矢印は作成時と選択後の両方で文字を設定できる。
- 引出線は先端、折点、文字位置を個別に編集できる。

### 北マーク

- 測量図で使われる専門的な北マークを採用する。
- 参照画像のような方位記号を基準に、角度、大きさ、色、線幅を調整可能にする。
- 配置前プレビューと配置後編集を同じ設定で行う。

### 家屋

- 配置前と選択後の両方で大きさを調整。
- 文字の大きさを図形サイズと別に調整。
- 中の色、枠線色、線種、斜線の有無・間隔・角度を設定。
- 幅、奥行、角度、寸法表示も再編集可能。

### 駐車

- 配置前と選択後の両方で大きさを調整。
- 文字の大きさを図形サイズと別に調整。
- 枠線色と中の色を独立して変更。
- 線種、幅、奥行、角度、寸法表示も再編集可能。

### 面積表

- 対象区画、列、書体、罫線、更新方法を専用画面で編集。
- 動的リンクと固定スナップショットを区別。
- 区画㎡、坪、価格、合計を正しく扱う。

---

## 11. ガイド・平行線

### 均等ガイド

- 線上に「2等分／区間／全長」等の説明文字を描かない。
- 必要な設定値は上部UIまたは選択後専用画面で確認する。
- 元の2点、分割数を保存し、選択後に再編集できる。

### 平行線

- 線上の「右側」「左側」という文字は表示しない。
- UIの操作名は「右側へ反転」等ではなく「反転」にする。
- 線は最初の基準線より十分長く表示し、実質無限線として扱える見た目にする。
- 保存する情報:
  - 基準線の2点
  - 間隔
  - 反転符号
  - 本数または繰返し番号
  - 表示スタイル
- 選択後に同じ条件を再編集できる。
- 出力ではガイド・平行線を初期非表示とし、必要な場合だけ「補助線を出力」をONにする。

---

## 12. 囲い選択

選択モードで、空白からクリックしたままドラッグして複数対象を囲えるようにする。

### CAD型の操作

- 空白から4pxを超えてドラッグしたときだけ囲い選択を開始。
- ドラッグ方向に関係なく、枠内へ完全に入った対象だけを選択する。触れただけの図形を含めず、意図しない一括編集・削除を防ぐ。
- 空白をクリックしてドラッグしなかった場合は選択解除。
- Shiftを押しながら囲うと既存選択へ追加し、Ctrlクリックは個別対象の選択を反転する。
- 図形本体、名称、面積、坪、寸法文字のどこをCtrl／Shiftクリックしても同じ追加規則にする。
- 選択済み図形本体からドラッグした場合は直接移動を優先。
- 頂点・ラベルから開始した場合は既存の頂点・文字移動を優先。
- 中ボタンまたはSpace+ドラッグはパン。
- Esc、右クリック、pointercancelで囲い操作を取消。
- 囲い取消時は開始前の選択集合へ戻し、Undo履歴を増やさない。
- 現在ページだけを対象にする。
- 非表示対象と下絵は対象外。
- 文字ラベルを囲った場合は所有オブジェクトを選択。
- 上部へ `編集中: 区画3件`、`辺2を個別編集`、`異種3件を共通編集` のように対象を常時表示する。

### 当たり判定

単純な頂点の外接矩形ではなく、実際に描画される範囲を使う。

- 回転・縦書き文字
- 道路名と幅員
- 面積・坪・辺寸法
- 矢尻
- 北・家屋・駐車
- 面積表
- 平行線の延長表示
- 線幅

---

## 13. 一覧・台帳

- 区画、道路、水路を分けた面積㎡・坪合計を表示。
- 区画+道路+水路の任意組合せ合計も表示。
- 既存実装には「一覧に種類別面積集計」が含まれるため、再実装せず挙動を試験する。
- 一覧行の文字は読みやすく大きくする。
- 行の縦余白は縮め、同じ高さに多く表示する。
- 独立した「一覧」レールボタンが不要という既指示を維持する場合、一覧・台帳への入口名を1つに統一する。
- 「検索」「一覧検索へ」「一覧・台帳」の3名称を統一する。
- 区画の番号・価格は道路・水路には表示しない。
- タブ、検索、種類絞込を変えた時は画面外の選択をクリアする。選択を保持する設計にする場合は、`非表示を含むN件`と削除対象を常時表示し、見えない対象を黙って削除しない。

---

## 14. 表示設定

- 「表示」のグリッドは、目に見える格子ではなく吸着機能なので、通常UIから削除する。
- 旧文書でgridがONでも、通常起動ではfalseへ正規化する。
- 吸着は頂点、交点、辺を中心にする。
- 将来残す場合だけ、詳細設定へ「格子点へ吸着」という正しい名前で置く。
- ズームの100%表示はクリック可能にし、編集用の標準表示倍率へ戻す。モニターの物理DPIを校正していないため、画面上の100%を定規上の実寸とは呼ばない。
- ヘルプの「全体」は「全体移動」と「全体表示」に分ける。

---

## 15. 出力・用紙・印刷

この章は最優先で実装する。

### 15.1 ページ別出力データ

各ページに次を保存する。

    outputLayout: {
      paperSize,
      orientation,
      printScale,
      offsetMmX,
      offsetMmY,
      showFrame,
      showTitleFrame,
      includeUnderlay,
      includeGuides,
      initialized
    }

- 全出力設定はページ別outputLayoutを正とする。文書側には新規ページへコピーするoutputDefaultsだけを置き、既存ページへ後から暗黙反映しない。
- 図面名、作成者、会社名などの文字内容だけは文書共通のpaperMetaへ置いてよい。
- includeUnderlayと編集画面のbackground.visibleは別設定。下絵を編集画面で隠しても出力設定を勝手に変えず、その逆も行わない。
- includeUnderlayは下絵がある新規ページでは初期true、下絵がないページではfalse。
- includeGuidesの初期値はfalse。falseならプレビュー、外接範囲、PNG、PDF、印刷のすべてから補助線を除外する。
- initializedは「初回だけ内容中心をprintableRect中心へ置いたか」を示す。trueになった後は、図形編集、ページ切替、再読込で自動再中央化しない。
- 旧文書読込時は、現在の校正縮尺を初期printScale候補にするが、図形座標を変更しない。
- 旧document.paperは各page.outputLayoutへ移行し、printScaleはそのページのcalibration.mapScaleが正なら採用、なければ未設定、offsetは初回中央配置、無効値は既定値へ正規化する。
- 現行schema v5からtarget schema v6へ更新する。過去の破壊的「用紙中央へ」ですでに移動された図形は、元座標を復元する情報がないため自動修復できないことを移行警告へ記録する。
- ページごとに用紙、向き、縮尺、配置を独立保存する。
- Undo/Redo、保存、読込の対象にする。

### 15.2 共通変換式

プレビュー、PNG、PDF、印刷で同じ物理変換を使う。

    scaleMmPerWorld = page.calibration.mpp * 1000 / printScale
    paperPointMm = paperAnchorMm
      + (worldPoint - worldAnchor) * scaleMmPerWorld
    canvasPointPx = paperPointMm * dpi / 25.4

- worldPointは2次元ベクトル。offsetMmX/Yはworld原点を紙面上へ写す表現として保持してもよいが、実装関数はanchor式へ正規化する。
- renderer内部の紙面座標は左上原点、+Xを右、+Yを下とする。PDF writerだけが最後にPDFの左下原点へ変換し、計測式を二重反転させない。
- mppは必ず出力対象ページのpage.calibration.mppを使う。アクティブページ用の複製値や別ページの校正を参照しない。
- mpp > 0かつprintScale > 0だけを実寸出力可能とする。
- 2点校正済みと印刷縮尺設定済みは別状態で表示する。2点校正からprintScaleを自動推定しない。
- 校正またはprintScaleが未設定なら、PDF、実寸PNG、印刷を止め、該当ページの設定へ誘導する。タイトル欄へ未設定縮尺を表示しない。
- 出力だけのズーム表示は、この物理変換の後にプレビュー用倍率として掛ける。実測値と印刷縮尺へ混ぜない。
- 用紙寸法は1か所で管理する。
  - A4 = 210×297mm
  - A3 = 297×420mm
  - raster px = round(mm × dpi ÷ 25.4)
  - PDF pt = mm × 72 ÷ 25.4

### 15.3 図形全体の配置調整

- 現在の「図形を用紙中央へ」が全shape/entityの座標を変更する実装なら廃止する。
- 出力画面に「配置を調整」モードを作る。
- マウスドラッグで、下絵と全図形を含む出力グループ全体を動かす。
- 下絵を出力OFFにしたときは図形だけ見えるが、相対位置と元データは変えない。
- cursorは grab / grabbing。
- ドラッグ中はライブプレビュー。
- pointerupで1回だけUndo履歴へ確定。
- pointer captureを使い、Esc、pointercancel、lostpointercaptureで開始前へ戻す。取消時はdirtyもUndo履歴も増やさない。
- 「中央配置へ戻す」を用意し、紙全体ではなくprintableRectの中央へ置く。offset=0を「戻す」の意味にしない。
- 用紙や縮尺を変えたときは、変更前に紙面中央にあったworld anchorが、変更後も同じpaper mm位置へ来るようoffsetを再計算する。勝手に全体を再中央化しない。
- 中心維持の基準式:
  - worldAnchor = (oldPaperCenterMm - oldOffsetMm) ÷ oldScaleMmPerWorld
  - newOffsetMm = newPaperCenterMm - worldAnchor × newScaleMmPerWorld
- ドラッグ量はdevicePixelRatioやbacking canvasではなくcanvas.getBoundingClientRect()から換算する。
  - deltaMmX = deltaClientX × paperWidthMm ÷ canvasClientRect.width
  - deltaMmY = deltaClientY × paperHeightMm ÷ canvasClientRect.height
- 下絵ON/OFFで同じoffsetを保持し、再度ONにしても下絵の位置が跳ばない。
- 「全体を収める」は必要値と候補だけを先に表示し、ユーザーが適用したときだけ縮尺変更と中央配置を1回のUndoで確定する。
- 図形個別の選択・移動は出力画面では行わない。

### 15.4 ユーザー選択式の縮尺

出力→用紙で、よく使う縮尺を上から選ぶ。

頻用:

- 1:50
- 1:100
- 1:200
- 1:250
- 1:300
- 1:500
- 1:1000

建築向け:

- 1:5
- 1:10
- 1:20
- 1:25
- 1:30

その他:

- 1:75
- 1:150
- 1:400
- 1:600
- 任意入力

- マウスホイールで印刷縮尺を変えない。
- アプリは候補を示すだけで、自動適用しない。
- 1:1000を超える場合は、1、1.5、2、2.5、3、4、5、6、7.5、10 × 10の累乗というnice値系列から、必要値以上の最小候補を作る。必要値そのものの任意入力も併記する。

### 15.5 はみ出し・小さすぎ判定

- 緑: 収まる。使用率を表示。
- 赤: はみ出す。上下左右の方向とmmを表示。
- 黄: 小さすぎる。より大きく印刷できる候補を表示。
- 越境が0.5mmを超えたら赤。0.5mm以下はラスター丸め等の許容帯として表示上は収まり扱いにする。
- 見た目外接範囲の幅率と高さ率の大きい方がprintableRectの45%未満なら「小さすぎる」の初期判定とする。閾値は設定へ露出せず、実図面で検証して調整する。

モデル形状だけから求める必要縮尺分母の初期推定:

    max(
      realWidthM * 1000 / usableWidthMm,
      realHeightM * 1000 / usableHeightMm
    )

- この式は中央配置したモデル形状の初期推定に使う。現在offsetでの上下左右の越境量とは分けて計算する。
- 紙面固定文字・線幅・矢尻があるため、各候補縮尺でrenderer-awareな紙面外接範囲を再計算して最終判定する。
- 計算値を四捨五入せず、必要値以上となる次の標準縮尺へ切り上げる。例: 必要値が1:437なら1:500を提案する。ただし適用はユーザー操作で行う。

### 15.6 出力外接範囲

実際に描かれる見た目をすべて含める。

- ポリゴンと線
- 道路名・幅員
- 水路名・水路幅
- ㎡・坪
- 辺寸法・区間寸法
- 手動移動した文字
- 回転・縦書き文字
- 家屋・駐車・北
- 面積表
- 線幅と矢尻
- 出力ONの下絵

補助線は初期出力OFFとし、OFFなら外接範囲にも含めない。

最終的な収まり判定は紙面座標で行う。

    leftOverflow = max(0, printable.left - visual.left)
    rightOverflow = max(0, visual.right - printable.right)
    topOverflow = max(0, printable.top - visual.top)
    bottomOverflow = max(0, visual.bottom - printable.bottom)

### 15.7 用紙枠・タイトル欄

- A4とA3、縦と横で物理的な用紙寸法を正しく変える。
- 枠とタイトル欄はピクセルではなくmmで定義する。
- 初期定数は、安全余白5mm、枠位置は用紙端から5mm、枠内の図面余白2mm、タイトル欄高さはA4で12mm・A3で15mmとする。参照図面との目視QAで最終調整する。
- 枠OFFでも実プリンター用の安全余白5mmは残す。全面使用は将来の明示的な「フチなし」設定に分ける。
- 参考図面のように外枠を細く明瞭にし、下部タイトル欄を小さく実務的な比率にする。
- 「用紙枠を表示」をOFFにした場合、下部タイトル表も完全に消す。
- printableRectMm は、余白、枠の安全域、表示中の下部タイトル欄を除いた領域。
- タイトル欄の縮尺は校正値ではなく出力printScaleを表示する。
- 枠線のプレビュー解像度も低く見えないようにする。
- タイトル文字は原則1行。欄内に収まらない場合は2.5mmまで縮小し、それでも収まらなければ末尾を省略記号にする。完全な値は保存データに保持する。

### 15.8 画質と物理線幅

- PNGは300dpiでA4 3508×2480、A3 4961×3508相当を向きに応じて出力。
- PNGへ300dpiのpHYs密度情報として11811 pixels/meter、unit=meterを埋め込む。
- PDFは正しいMediaBoxを持つ。
  - A4: 595.276×841.890pt
  - A3: 841.890×1190.551pt
  - 横向きは幅と高さを入れ替える
- 現行のCanvas画像埋込PDFを当面使う場合でも、300dpi、物理線幅、日本語、下絵を正しく出す。
- 将来はベクターPDF生成へ移行可能なrenderer分離を維持する。
- 画面固定pxの線幅をそのまま300dpiへ使わない。
- 線、外枠、寸法、ハッチ、スタンプの線幅を紙面mmで定義し、各dpiへ換算した px ÷ (dpi ÷ 25.4) が同じ値になるようにする。モニター上の定規実寸は保証対象にしない。
- PDF下絵は、出力縮尺と必要dpiに応じて元PDFから再レンダーする。
- 1:500の下絵を1:100へ拡大しても固定低解像度画像を引き伸ばさない。
- PDF下絵の必要倍率は、outputPxPerWorld = mpp ÷ printScale × outputDpi ÷ 0.0254 を基礎に、下絵transformの最大倍率と1.1の安全率を掛けて決める。
- 最大画素数の制限で必要倍率を満たせない場合、黙って低画質にせず実効dpiを警告する。
- 画像下絵は再レンダーで高精細化できないため、sourcePixelWidth ÷ printedWidthInches で実効dpiを表示する。

### 15.9 印刷

- PDFを実寸出力の基準成果物とする。
- PDFカードを押したときはnative保存ダイアログ、成果物renderer、書込結果を1経路で接続し、成功時の絶対パスと失敗理由を画面へ表示する。クリックして無反応の状態を許さない。
- Windows印刷画面では「実際のサイズ／100%」「用紙に合わせるOFF」を案内する。
- プリンタドライバ側の自動Fitによる誤差をアプリの縮尺誤差と混同しない。

### 15.10 複数ページの書き出し

- 出力プレビューは現在の編集ページと連動し、ページ切替を表示する。
- PDFは「現在ページ」と「全ページ」を選べる。
- 全ページPDFは、ページごとのA3/A4・縦横・printScale・offsetをそのまま使い、混在MediaBoxを許可する。
- PNGは「現在ページ」と「全ページを連番ファイルで保存」を選べる。全ページ時は保存先フォルダを選び、文書名-page-001.pngのように出力する。
- 全ページ出力前に、未校正、printScale未設定、はみ出し、低実効dpiをページ別一覧で確認する。
- 現在ページだけの出力に、余計な空白ページや2ページ目を付けない。

### 15.11 実寸の例

- 10mを1:100で出力 = 100mm
- 10mを1:200で出力 = 50mm
- 17.555mを1:100で出力 = 175.55mm

この値をPDF上で機械測定し、目視だけで合格にしない。

---

## 16. 出力の受入試験

### 用紙

- A4縦、A4横、A3縦、A3横のMediaBoxが±0.01mm以内。
- 用紙変更でプレビューの縦横比と実ファイル寸法が変わる。
- 枠OFFでタイトル欄も消える。
- タイトル欄ON/OFFで使用可能領域の判定が変わる。
- 全ページPDFでA3/A4・縦横が混在しても各ページのMediaBoxが正しく、現在ページ出力に余計なページがない。

### 縮尺

- 10mと17.555mを1:50、1:100、1:200、1:500でPDF測定。
- 校正mppを変えずにprintScaleだけ変更できる。
- printScale変更後もshape/entityのworld座標、background transform、page calibration、面積、寸法値が完全一致し、変更される文書データはoutputLayoutだけ。
- Canvas埋込PDFでは検尺線の端点ピクセルをMediaBoxのmmへ換算して測る。許容は±2px、300dpiで約±0.17mm、縮尺相対誤差±0.1%以内。

### 配置

- 出力ドラッグ前後で全shape/entityのworld座標、background transform、全page calibrationがバイト単位で同じ。outputLayoutだけは変更を許す。
- 下絵と図形の相対位置が変わらない。
- Windows表示倍率100%、125%、150%、200%で、プレビュー紙幅に対する同じ割合のドラッグが同じpaper mm offsetになる。CSS rect換算を自動試験する。
- 配置はページごとに保存・復元。
- 1回のドラッグは1回のUndo。
- 用紙、向き、縮尺、中央配置、全体を収める、ドラッグを各1回のUndo単位にする。
- Esc、pointercancel、lostpointercaptureで取り消した場合、dirtyとUndo履歴が増えない。

### 画質

- PNG実ファイルを解析し、寸法とpHYs=11811 pixels/meter、unit=meterを確認。
- PDFをPopplerで再画像化し、文字、線、下絵、枠の欠けとぼけを確認。
- previewLinePx ÷ (96 ÷ 25.4) と outputLinePx ÷ (300 ÷ 25.4) が、同じpaper-mm線幅になる。
- PDF下絵を大きく出力しても固定プレビュー画像より明瞭。
- PDFのImage XObjectの画素数、実効dpi、ページ数を実ファイルから検査する。
- 1:500のPDF下絵を1:100へ拡大出力し、必要な再レンダー画素数と実際の埋込画素数を比較する。

### 外接範囲

- 回転文字、縦書き、幅員、辺寸法、北、家屋、駐車、表、矢尻がはみ出し判定へ入る。
- 出力OFFのガイド・下絵は判定から除外。
- はみ出し量と推奨縮尺が手計算と一致。

### プリンタ実機

- PDFビューアで100%、用紙に合わせるOFF。
- 定規で100mm検尺線を測り、±0.5mm程度を目安にする。アプリ計算誤差とプリンタ搬送誤差は別々に記録する。

---

## 17. UIの既知改善項目

実装時に漏らさない。

- 出力画面の初期タブは、用紙未設定時だけ用紙、それ以外は書き出しを候補とする。
- Ctrl+Pで印刷。
- primary/dangerボタンのhoverで白文字が消えない。
- 空のキャンバスへ「PDF・画像をドロップ」の案内。
- 選択中の図形本体をドラッグ移動。微小誤操作防止のしきい値あり。
- 選択中の頂点ハンドルを直接ドラッグ。
- 「頂点」は「頂点を編集」へ。
- 文字の既定「文字」をそのまま誤配置しにくい導線。
- 右一覧の文字を大きく、行をコンパクトに。
- 無効な項目を残さず、種類に不要なら消す。

---

## 18. 実装フェーズ

### Phase 0: 仕様・データ境界を固定

- この文書を基準に、現行コードとの差分表を作る。
- schema versionとmigration方針を決める。
- outputLayout、道路・水路の文字役割、計測表示、親子関係、parallel/guide再編集情報を定義。
- 現在のユーザー変更を保護。

完了条件: データ項目とUndo単位が文書化され、実装前後の変換方針が決まっている。

### Phase 1: 保存・出力コア

- 保存前commit、現在パス、開く、未保存確認。
- calibrationとprintScaleを分離。
- ページ別outputLayout。
- 共通mm変換。
- 破壊的centerDrawingを撤去。
- 出力グループのドラッグ。
- 外接範囲、はみ出し、小さすぎ、候補。
- 物理線幅、PDF下絵再レンダー、PNG密度。
- 実寸・MediaBox試験。

完了条件: 出力操作で図面データが変わらず、PDF実寸が合う。

### Phase 2: 種類別選択UIと文字基盤

- 全18種類の編集ルーター。
- 単一・同種複数・異種複数。
- 書体token統一。
- 作成時と選択時の設定対応表。
- 無効UI削除。

完了条件: どの対象を選んでも、必要項目だけが同じ場所に出る。

### Phase 3: 区画・道路・水路・隅切り

- 種類変更。
- 日本語名、番号削除、色。
- 道路名・幅員、水路名・水路幅。
- 任意の辺寸法、㎡、坪、約。
- 分割、一括分割、合筆、隅切り復元。
- 一覧集計。

完了条件: 3種類が完全分離されつつ安全に変換・分割・集計できる。

### Phase 4: 計測・寸法

- 作図中ライブ寸法。
- 線上側の既定位置。
- 全体・個別設定の統合。
- 2点、折れ線、面積、寸法の保存・Undo。

完了条件: 作図中と確定後で数値・位置・書式が一貫する。

### Phase 5: 注記・スタンプ・表・ガイド

- 文字、矢印、引出線。
- 北、家屋、駐車。
- 面積表。
- guide、parallel。
- 配置前と選択後の編集対称性。

完了条件: すべての作成設定を選択後に再編集できる。

### Phase 6: 囲い選択・移行・総合QA

- CAD型囲い選択。
- renderer-aware bounds共用。
- 旧JSON migration。
- 全種類の作成→編集→Undo→保存→読込→出力。
- 900×600、IME、複数ページ、破損データ試験。

完了条件: 全QA通過、GUI起動スモーク、実機出力確認。

### Phase 7: リリース記録と配布

- CHANGELOGへ実装内容、互換性、既知制限を記録。
- GitHubへ今回分だけを意図的にコミットし、対象ブランチへ反映。
- Notionの開発ログへ、変更点、試験結果、残件を記録。
- electron-builder-v210.ymlを使ってWindows x64インストーラーを作成。
- インストーラーの起動、上書き更新、保存・読込、PDF出力をクリーン環境相当でスモーク試験。
- 配布物の版番号、ファイル名、サイズ、SHA-256を記録。
- GitHub・Notionへの外部反映は、実装とQA完了後に実行内容を再確認して行う。

完了条件: ソース、変更履歴、開発ログ、インストーラーが同じ版番号と試験結果を指している。

---

## 19. 実装しない・暗黙に行わないこと

- PDFの縮尺を文字やページサイズから自動検出・自動適用しない。
- 出力で図形座標を動かさない。
- はみ出し時に縮尺を自動変更しない。
- 出力縮尺を校正mppへ書き戻さない。
- 道路へ英語roadを入れない。
- 道路・水路に区画番号や価格UIを出さない。
- 使えないUIをdisabledのまま大量に並べない。
- 保存前の入力を捨てない。
- グリッド吸着を見える表示設定のように見せない。
- 異種複数選択で固有属性を一括変更しない。
- 旧試作コードをv210へ安易にimportしない。

---

## 20. 実装再開時の手順

会話が圧縮された場合、次の順で再開する。

1. このファイルを最初から最後まで読む。
2. git status、現在ブランチ、最新コミットを確認する。
3. V210_FULL_FEATURE_SPEC.md と ui-usability-review-v210.md の未反映差分を確認する。
4. 今回触るファイルの既存差分を確認し、ユーザー変更を保護する。
5. 上の優先順位で、未完了の最上位フェーズだけを実装する。
6. 変更前に失敗する再現試験またはQAを追加する。
7. 実装後、関連QA、全QA、GUI起動スモークを行う。
8. この文書の該当項目へ完了状況と確認結果を追記する。
9. ユーザーが実操作テストを行う段階ではElectronアプリを起動し、変更内容、未完了、試験結果、起動状態を報告する。

---

## 21. 完了状況の記録欄

alpha.10の対象範囲は現行コードと最新QAで再監査した。過去のalpha.9配布記録は履歴として残し、alpha.10の配布・外部反映は成果物確定後に記録する。

確認済み:

- [x] v2.1.0-alpha.8 の対象リポジトリとブランチを特定
- [x] 種類別面積集計のコミット 716e40c を確認
- [x] 本マスター計画を作成
- [x] schema v7、ページ別outputLayout、旧形式移行、種類固有の`kindStates`を確認
- [x] A4/A3縦横のElectron PDF MediaBoxと300dpi PNGの画素数・pHYs情報を実成果物で確認
- [x] 出力配置ドラッグ、中央配置、はみ出し・小さすぎ・縮尺候補を実装
- [x] 旧期待値でのv2.1 QA 296/296件・実操作QA 19/19件合格記録を確認（2026-07-18）。次期仕様の合格判定には再利用しない

alpha.10で確認済み:

- [x] 保存・開く・未保存確認、未確定入力のflush、原子的上書き、案件間draft分離
- [x] 下絵のページ別縮尺、直接設定と2点校正の再編集、ページ切替時の状態保持
- [x] 区画・道路・水路の種類別固定UI、作図時と選択後の項目・順序・意味の対称性
- [x] 道路・水路・区画変換と、schema v7 `kindStates`による種類固有値の保持
- [x] 分割・合筆・隅切りと、分割後の図形・辺ID一意性および保存・再読込
- [x] 文字・寸法・スタンプ・面積表・ガイドの固定メニュー化と入力トランザクション
- [x] ゴシック・明朝・均等、名称・幅員・面積・坪・辺寸法の対象別文字設定
- [x] 独自の浮動ポップアップを極小化し、通常の属性編集から確定・適用・更新ボタンを撤去
- [x] A4/A3縦横のPDF MediaBox、用紙枠・タイトル帯の連動、300dpi PNGの物理解像度
- [x] 最新仕様へ期待値を更新した総合QA 299 / 299項目合格
- [x] 実操作QA 21 / 21項目合格、コンソールエラー0件
- [x] CHANGELOGとインストーラーをalpha.9へ統一（87,668,693 bytes、SHA-256 3CFEC516BC10CAE6D98D07B8BE2C7A0258BD0F008066206C995218A541D09798、展開版起動確認済み）
- [x] Notionへalpha.9開発ログを作成（ページID 3a1c75d6-89bc-8184-980a-d368d244c393）
- [x] GitHubへalpha.9を反映（GitHub CLI認証完了、対象ブランチと既存PRへ反映）

alpha.10の配布工程:

- [x] Windows x64インストーラー作成・起動確認と、サイズ・SHA-256記録
- [x] GitHubへalpha.10を反映（commit `c989efb`、Draft PR #1）
- [x] Notionへalpha.10開発ログを反映（ページID `3a1c75d6-89bc-819f-991a-d39d269b68ec`）

---

## 22. 2026-07-18 再棚卸し: 区画・道路・水路・選択・縮尺再設定

状態: **alpha.10で対象範囲を実装済み。総合QA 299 / 299項目合格。**

対象は、区画・道路・水路の作成時と選択後のメニューバー、文字・面積・坪・幅員・辺寸法の編集、および設定済み縮尺の再設定である。機能を足す前に、同じ意味の操作が同じ場所・同じ表記・同じ階層に出ることを最優先にする。

### 22.1 実装前UIの診断（履歴）

| 領域 | 現行 | 問題 |
|---|---|---|
| 作成時 | 区画は色・表示が直置き、数値と文字がポップアップ。道路・水路は種類・幅員が直置き、色と文字がポップアップ | 同じ図形作成でも、項目の置き場所と順番の規則が違う |
| 選択後のページ | 区画は「基本・面積/坪・辺寸法・表示・文字・台帳」、道路・水路は「情報・名称/幅員・辺寸法・面積/坪・表示・文字」 | ページ順、名称、役割が種類ごとに変わり、記憶できない |
| 文字位置 | 「中央へ戻す」は機能として残っているが、「文字」ページ内の「文字装飾」ポップアップに入っている | 消えたように見える。位置操作を装飾の中へ置く分類も不自然 |
| 道路・水路文字 | 名称の文字書式は「文字」、幅員・水路幅の書式は別の専用ページ、面積・坪はさらに別ページ | 何の文字を編集しているかが明示されず、題名だけ変わるように見える |
| 道路・水路名称 | 基本ページと「名称・幅員」ページの両方に同じ名称入力がある | 同じ値の編集口が重複し、どちらが正式な入力か分からない |
| 辺寸法 | 「全辺共通」は共通フォームだが、「辺1～」を選ぶとフォーム全体が別構成へ置き換わる | 表記、並び、ポップアップの数、戻す操作が編集対象で変わる |
| 面積・坪書式 | 角度・縦書き・色は保存処理に存在するが、現行画面からは編集できない | 内部仕様と表示UIが一致していない |
| タブ装飾 | 固定6ボタンを内容だけ差し替え、見た目用クラスを使い回している | 強調枠が実際のページ以外へ付く場合がある |
| 道路→水路切替 | 名称・幅・色は水路用へ変わるが、書体・文字サイズ・縦書きの既定は完全には切り替わらない | 種類を変えても道路の文字設定が残る場合がある |
| 「均等」書体 | 現行の `mono` は等幅フォントの選択で、文字列を指定幅へ均等配置する機能ではない | 「均等割付」と「等幅フォント」が混同されている |
| 文字の直接選択 | 面積・坪・辺寸法以外の文字をクリックしても、原則「基本」ページへ移る | 文字を選んだのに文字編集が開かず、対象と結果が一致しない |
| 編集階層 | 上位ページ、ページ内の子タブ、さらにポップアップが混在 | 3階層になる場所があり、同じ操作を探す負担が大きい |
| 縮尺 | 候補・直接入力と2点校正が同じ列に「または」で並び、適用すると直ちに選択画面へ戻る | 設定済み状態から方法を切り替えられることが伝わらず、再設定不能に見える |
| 縮尺入口 | 「縮尺を合わせる」「ページ・縮尺」「縮尺 設定済」が別入口・別表記 | 同じ画面へ行く操作だと認識しづらい |

結論: 中央へ戻す機能そのものの欠落だけではなく、**同じ属性を編集するUIが複数の設計思想で継ぎ足されていること**が主因である。個別のボタンを戻すだけでは再発するため、共通編集モデルへ揃える。

### 22.2 統一する設計原則

1. 区画・道路・水路は、選択後のページ位置を共通化する。
2. 上位ページの中に子タブを作らない。ページ内の切替は必ず「編集対象」1個で行う。
3. 区画・道路・水路・選択・縮尺では、独自の浮動ポップアップを原則使用しない。設定はメニューバー内の固定行へ表示する。
4. 「中央へ戻す」「自動位置へ戻す」「表示」「編集対象」は隠さない。
5. 種類に無効な項目はdisabledで残さず、表示しない。
6. 作成時と選択後は同じ項目名・同じ順序・同じ共通部品を使用する。
7. 書式の対象を曖昧にせず、「区画名」「道路名」「幅員」「水路名」「水路幅」「面積」「坪」「辺寸法」と明記する。
8. 選択対象や編集対象を切り替えても、フォームの骨格とボタン位置を動かさない。
9. 「ゴシック」「明朝」「均等」を全ての文字役割で同じ選択肢として扱う。「均等」は単なる等幅フォントで代用せず、実際の均等割付として仕様・描画・保存を統一する。
10. 図面上の文字を直接選択した場合は、「文字」ページを開き、その文字役割を自動で編集対象にする。辺寸法を直接選択した場合は「辺寸法」を開く。

### 22.3 選択後メニューバーの統一案

区画・道路・水路で `基本｜表示｜文字｜面積・坪｜辺寸法｜台帳` の順序を固定する。台帳は区画だけに表示するが、それ以前のページ位置は動かさない。

1. **基本**
   - 共通: 頂点を編集、種類変更。
   - 区画: 番号、区画名。
   - 道路: 道路種別、道路名、幅員。
   - 水路: 水路名、水路幅。
   - 道路名・水路名の入力欄はここだけに置き、他ページでは重複編集しない。
   - 種類変更時は、名称を「区画／道路／水路」の日本語既定へ切り替え、道路・水路では区画番号を削除する。変更先を選んだ時点で実行し、確認ボタンは置かず、1回のUndoで元へ戻せるようにする。
2. **表示**
   - 塗り、外枠、濃さ。
   - 表示項目を対象別に並べる。区画は番号・区画名・面積・坪・辺寸法、道路・水路は名称・幅員または水路幅・面積・坪・辺寸法。
3. **文字**
   - 先頭に共通の「編集対象」セレクトを置く。
   - 区画: 区画名／面積／坪。
   - 道路: 道路名／幅員／面積／坪。
   - 水路: 水路名／水路幅／面積／坪。
   - 常時表示: 書体、文字サイズ、色、対象ごとに独立した「位置を自動へ戻す」。
   - 詳細: 角度、縦書き、枠、下線。対象に関係しない項目は出さない。
   - 道路名と幅員、水路名と水路幅の位置・書式は独立させる。「名称に合わせる」を用意する場合も、連動中であることを明示する。
4. **面積・坪**
   - 「面積㎡／坪」の編集対象を切り替える。
   - 自動計算値、手入力表示文字、約、小数桁、丸め、補正を扱う。
   - 書体・大きさ・色・位置は「文字」へ統一し、このページに重複させない。
5. **辺寸法**
   - 先頭に共通の「編集対象」を置き、「全ての辺（共通設定）」「辺1（12.34m・個別設定）」「辺2（…）」の表記に統一する。
   - 編集対象を変えても同じ位置に、表示、表示文字、書体、サイズ、約・切捨て、位置を自動へ戻すを表示する。
   - 詳細には、小数桁、丸め、補正値、色、角度を置く。
   - 「全ての辺」は既定値、「辺1～」はその辺だけの上書きとして扱い、「個別設定を共通へ戻す」を明示する。
6. **台帳**
   - 区画だけに表示する。価格・メモ・一覧関連を置き、図面文字編集と混ぜない。

道路・水路の「名称・幅員」上位ページと、その中の「内容／角度／書式」子タブは廃止する。面積・坪は数値内容だけのページとして残し、文字の見た目は共通の「文字」ページへ集約する。

### 22.4 ポップアップを原則使わないメニューバー

選択後のメニューバーは次の固定構造にする。

- 1段目: 選択解除・移動・複写・削除などの共通操作と、`基本｜表示｜文字｜面積・坪｜辺寸法｜台帳`。
- 2段目: 編集対象、表示ON/OFF、名称・表示文字・数値、位置を自動へ戻す。
- 3段目: 書体、文字サイズ、色、角度、縦書き、枠、下線、小数桁、丸め、補正値など、そのページの書式項目。
- 横幅に余裕がある時は2・3段目を同じ行へ詰め、狭い時は固定順のまま折り返す。浮動パネルや横スクロールで隠さない。

色は主要色の小さなスウォッチをバー上へ並べる。任意色が必要な場合だけ標準の色選択を使う。編集対象・書体・丸めなどは通常のセレクトを使用してよいが、独自の浮動パネルや子タブにはしない。

原則として使わないもの:

- `<details>`型の「文字」「文字装飾」「数値」「書式」「色」ポップアップ
- 上位ページ内の「内容／角度／書式」子タブ
- ポップアップ内の編集対象切替
- 全ての辺と個別辺でのツールバー全体の差替え
- 重要操作を「詳細」に隠す構成

例外として許可するもの:

- 任意色を指定するためのOS標準色選択
- ファイルを開く・保存するためのOS標準画面
- 新規作成、別ファイル読込、アプリ終了で未保存内容が失われる場合の固定確認バー。文書を置き換えるためUndoで救済できない操作に限る。

種類変更、合筆、削除などUndoで戻せる図面編集には確認画面を出さない。決定規則を明示して即時実行し、ステータス欄へ結果と `Ctrl+Zで戻せます` を表示する。

用語も固定する。「辺長／寸法」は「辺寸法」、「全辺共通」は「全ての辺（共通設定）」、「小数／桁」は「小数桁」、「補正／寸法値」は「補正値」、「中央へ戻す／元の位置へ戻す／自動位置へ戻す」は「位置を自動へ戻す」に統一する。位置ボタンは既に自動位置でも消さず、無効状態で同じ場所に残す。

#### 確定ボタンを置かない操作規則

選択後の属性編集には「確定」「適用」「更新」ボタンを置かない。

- チェックボックス、セレクト、色スウォッチ、切替ボタンは、操作した時点で反映・保存する。
- 文字・数値入力は入力中に図面へプレビューし、Enter、フォーカス移動、IMEの変換確定時に1回の編集として確定する。
- 入力欄へフォーカスした時点の値を保持し、1回の入力操作を1回のUndoへまとめる。1文字ごとにUndoを増やさない。
- Escは入力開始前の値へ戻す。
- 不正な数値や空欄途中は文書へ保存せず、入力欄の横へ理由を表示する。警告ダイアログは出さない。
- 「選択へ戻る」は確定ではなく画面移動として残す。保存、PDF出力、削除など、実行すること自体が目的のボタンは残す。

作図中も、別の確定ボタンに依存しない。

- 2点距離、寸法、固定点数の作図は最後の点をクリックした時点で作成する。
- 区画、道路、水路、折れ線、面積などの多点作図は、最終点のダブルクリックまたはEnterで終了する。
- 右クリックまたはBackspaceで1点戻し、Escで作図を取り消す。
- マウス操作だけでも完結できるよう、ダブルクリック終了を必ず維持する。

### 22.5 作成時との対称性

作成時も、選択後と同じ属性グループと表記を再利用する。

- 区画作成: 基本属性、表示、文字、辺寸法。
- 道路・水路作成: 基本属性、表示、文字、辺寸法。
- 作成時に指定できた書体、サイズ、色、縦書き、面積・坪・辺長表示、約設定は、選択後も同じ名称で変更できる。
- 道路から水路、水路から道路へ種類を変えた際は、名称だけでなく書体・文字サイズ・色・縦書き・幅文字設定も変更先の既定へ切り替える。変更は即時実行して1回のUndoへまとめ、確認ボタンは出さない。
- 作成時に未指定の位置調整だけは、配置後に「中央へ戻す／自動位置へ戻す」として出す。
- HTMLテンプレートを種類ごとに複製せず、属性定義から同じ部品を生成する。これにより将来の追加時も片側だけ更新される事故を防ぐ。

### 22.6 縮尺再設定の統一案

「ページ・縮尺」を唯一の画面名とし、設定メニュー、左側ボタン、ステータス欄も同じ名称にする。

画面構成:

1. 現在ページと現在値を常時表示する。例: `2ページ / 現在 1:100（候補設定）`、`2ページ / 現在 2点校正`。
2. 方法を「縮尺値で設定」「2点校正（既知距離）」の2つから明示的に選ぶ。「2点距離」は計測機能名と重なるため縮尺画面では使わない。
3. 設定済みでも両方を編集可能にし、`1:100 → 2点校正`、`2点校正 → 1:200`を相互に変更できる。
4. 縮尺候補は選択変更時、直接入力はEnterまたはフォーカス移動時に反映する。「適用」「確定」ボタンは置かない。
5. 2点校正へ切り替えた時点では既存縮尺を破棄せず、2点と実距離が揃い、実距離入力が確定した時点で置き換える。
6. 既に図形があるページで変更する場合は、「このページの寸法・面積が再計算されます」と表示する。図形座標や下絵位置は動かさない。
7. 画像DPIが不明で候補・直接入力を使えない場合だけ、その方法を隠して理由を表示し、2点校正へ案内する。
8. 1回の有効な入力確定を1回のUndo単位にし、ページ別縮尺の保存・読込を維持する。
9. 「2点を選び直す」は2点校正モードで常時見える位置に置く。設定解除は低頻度のため詳細内に置く。
10. 2点校正モードには「校正点を選ぶ」「1点戻す」「最初から」を明示し、画面を開いただけでキャンバスクリックが暗黙に校正点になる状態をなくす。
11. 校正データに設定方法を保存し、ステータス上で「縮尺値で設定」「2点校正」を区別する。
12. 未設定の作図から縮尺画面へ誘導された場合だけ有効値の確定後に元の作図へ復帰し、ユーザーが明示的に開いた再設定では「選択へ戻る」まで画面に留まる。

### 22.7 実装順

#### Step A: 共通仕様と部品を固定

- 区画・道路・水路の属性対応表をテストデータとして定義する。
- ページ順、編集対象、常時表示項目、詳細項目、位置リセット文言を定数化する。
- 固定6ボタンの使い回しをやめ、編集ルート定義からタブDOMと装飾クラスを同時に生成する。
- 「等幅」と「均等割付」のデータ・描画仕様を分離し、既存データの移行規則を決める。
- 既存の作成時／選択時フィールドと保存データの対応表を作り、移行で属性を失わないことを確認する。

完了条件: 3種類の画面をコード分岐ではなく同じ定義から生成できる設計になっている。

#### Step B: 選択後UIを先に統一

- 固定ページ順へ変更。
- 道路・水路の名称入力を基本ページへ一本化し、「名称・幅員」の子タブを廃止する。
- 面積・坪ページは数値内容だけに整理し、書式と位置は「文字 > 編集対象」へ統合する。
- 「位置を自動へ戻す」を常時表示へ戻し、道路名・幅員、水路名・水路幅を独立して戻せるようにする。
- 全辺共通と辺別で同一フォームを使い、値の参照先だけを切り替える。
- 図面上の文字・辺寸法を直接クリックした時、対応ページと編集対象を自動で開く。

完了条件: 区画・道路・水路のいずれでも、同じ場所を見れば目的の文字と辺寸法を編集できる。

#### Step C: 作成時UIを同じ部品へ統合

- 区画作成、道路・水路作成の表示順と表記を揃える。
- 作成時設定と選択後設定の往復試験を追加する。

完了条件: 作成時に設定した全項目を、選択後に同じ名称で再編集できる。

#### Step D: 縮尺の再設定フロー

- 方法切替、現在値表示、入力確定時の即時反映、「選択へ戻る」を実装し、適用・確定ボタンを撤去する。
- 校正方法を保存し、現在ページの取得・置換・解除を共通関数へ集約する。
- 2点校正の開始、1点戻す、最初からを実装する。
- 既存図形がある場合の再計算案内とUndoを実装。
- ページ別保存を維持する。

校正データは最低限 `method: scale | two-point`、`mapScale`、`mpp`、`points`、`realDistanceM` を持つ。現在ページの校正を直接複数箇所から書き換えず、取得・置換・解除の共通経路を使う。

完了条件: 同じページで候補設定と2点校正を何度でも相互に変更でき、Undo・保存・読込後も一致する。

#### Step E: 回帰QAと実操作確認

- 区画・道路・水路それぞれで、作成→選択→全文字対象編集→位置移動→位置リセット→保存→読込→Undo/Redoを確認する。
- 道路名・幅員、水路名・水路幅、面積・坪が独立し、同じ名称入力が2か所に出ないことを確認する。
- 区画名・道路名・水路名・幅員・水路幅・面積・坪・辺寸法の全てで、ゴシック・明朝・均等、サイズ、色、角度、縦書きの対応範囲を確認する。
- 全辺共通→辺1→辺2→全辺共通の切替で、フォーム位置と表記が変わらず、個別上書きだけが正しく切り替わることを確認する。
- `候補設定→2点校正→Undo→Redo→候補設定`、ページ切替、保存・読込を確認する。
- 2点校正の1点目取得後に、1点戻す・最初から・キャンセルが旧縮尺を壊さず動くことを確認する。
- 900px、1366px、1920px幅、Windows表示倍率100%～200%で、固定2～3段のメニューバーが欠けず、横スクロールや画面外操作を要求しないことを確認する。

完了条件: 自動QA、Electron実画面、ユーザー実操作の3段階で統一性を確認する。

### 22.8 今回の非変更範囲

- この棚卸し段階では、v210のHTML・CSS・JavaScriptを変更しない。
- 既存保存データの移行は、実装時にテストを先に追加してから行う。
- 個別ボタンだけを先行移動せず、Step Aの共通ルール確定後にまとめて変更する。

### 22.9 全機能横断棚卸し: ポップアップ・確定・入力反映

状態: **alpha.10で固定メニューバー、ポップアップ極小化、入力終了時の自動確定を実装済み。**

この節は、独自ポップアップを原則使わないこと、属性編集の確定・適用・更新ボタンを置かないこと、入力終了時に1回だけ保存することに関して、前節までの記述より優先する。

#### 22.9.1 浮動UIの全件分類

| 現行UI | 現在の範囲 | 方針 |
|---|---|---|
| `toolbar-dropdown` | 静的12定義、動的7定義。区画、道路、水路、計測、スタンプ、選択後の表示・文字・寸法、文字サイズ、面積表、家屋・駐車に存在 | 全19定義を撤去し、選択中ページの固定2～3段へ展開する |
| 独自色パレット | 静的な色select 10か所と動的生成経路。元selectを隠して浮動パネルを生成 | 独自パネルを撤去し、色名と現在色が分かる通常selectを表示する。任意色だけOS標準色選択を許可 |
| 縮尺未設定モーダル | 縮尺が必要な作図開始時に画面中央へ表示 | 撤去。ページ・縮尺へ自動遷移し、理由を固定メニューバー内へ表示する。Escで元の選択へ戻る |
| 道路・水路合筆の `window.confirm` | 名称・幅・色が異なる2図形を合筆する時 | 撤去。先に選んだ図形の属性を採用して即時実行し、ステータス表示とUndoで戻す |
| 未保存確認バー | 新規作成、別ファイル読込、下絵の新規読込、終了 | 維持。非モーダルの固定バーであり、履歴を破棄してUndoできない操作だけの安全例外 |
| 上部のファイル・編集・作図等のアプリメニュー | 低頻度の全体コマンド | 維持。一般的なデスクトップメニューとして意味が明確で、属性編集ポップアップとは分ける。内部に属性設定を入れない |
| カテゴリランチャー、編集ページ、一覧・出力・ヘルプのタブ | 固定コマンド領域内の主画面切替 | 維持。画面上へ浮かず、現在位置が常時見えるためポップアップ扱いにしない |
| OSファイル選択・保存・任意色・Windows印刷 | 外部ファイルまたはOS機能 | 維持。独自画面へ置き換えない |

撤去対象の内訳:

- 区画作成: 寸法値、文字。
- 道路・水路作成: 色、文字。
- 計測作成: 数値表示。
- 北・家屋・駐車作成: 配置詳細、表示。
- 選択後: 表示項目、文字装飾、数値設定、辺・区間寸法の数値・書式、家屋・駐車の配置詳細・表示、面積表の表示・配置詳細。
- 全体文字サイズ: 個別調整。
- 面積表配置: 表示、配置詳細。

固定バー化の規則:

- 1段目は共通操作とページ切替。
- 2段目は対象・内容・表示・位置。
- 3段目は書体・サイズ・色・角度・縦書き・数値形式。
- 横幅が狭い時だけ固定順で折り返す。浮動、横スクロール、画面への重なりは使わない。
- 900×600、1366×768、1920×1080、Windows表示倍率100～200%を対象にする。

#### 22.9.2 確定系UIと新しい終了条件

| 対象 | 現行 | 新仕様 |
|---|---|---|
| 区画・道路・水路・選択後の属性 | 入力中プレビュー、主にchangeで保存。種類変更だけ別ボタン | select・check・色は操作時に即時保存。文字・数値はEnter／フォーカス移動／IME入力終了で1回保存。種類変更ボタンは撤去 |
| 縮尺候補 | 候補選択後に「このページに設定」 | 候補を選んだ時点で現在ページへ保存 |
| 任意縮尺 | 入力後に設定ボタンまたはEnter | Enterまたはフォーカス移動で保存。無効値は保存せず欄横表示 |
| 2点校正 | 2点、実距離、2点で確定 | 2点と有効な実距離が揃った最後の操作で保存。入力順は問わない |
| 下絵位置調整 | 数値・ドラッグは既に保存され、「調整を終了」が別にある | 終了ボタンを撤去。Esc、選択へ戻る、別ツール選択でモードを離れる |
| 区画・道路・水路・折れ線・面積・線 | 確定ボタン、ダブルクリック、Enterが併存 | 確定ボタンを撤去。ダブルクリックまたはEnterで終了 |
| 選択分割・一括分割 | 確定ボタン、ダブルクリック、Enterが併存 | 確定ボタンを撤去。分割線のダブルクリックまたはEnterで終了 |
| 2点距離・矢印・引出線 | 2点目で既に自動作成 | 現行を正式仕様とし、共通テンプレート内の潜在確定ボタンを削除 |
| 文字・北・家屋・駐車・面積表 | 配置位置クリックで既に自動作成だが、潜在配置ボタンも存在 | 配置位置クリックだけで作成。配置ボタンを削除 |
| 合筆 | 2件目選択で自動作成。属性差異時だけ確認 | 2件目選択で即時作成。先に選んだ属性を採用し1 Undo |
| 隅切り | 区画・頂点選択後に確定／Enter | 寸法を先に入力し、対象頂点のクリックで即時作成。ホバー中に結果をプレビュー |
| 2点間均等ガイド | 2点目で既に自動作成 | 現行維持 |
| 区画等分線 | 区画＋方向2点の後に作成／Enter | ポインタ移動中に分割線をプレビューし、方向2点目のクリックで作成 |
| 平行線 | 基準2点後に作成／Enter。反転後も同じ作成操作 | 基準2点後は距離変更と反転をライブプレビューへ反映し、プレビュー線側のキャンバスクリックで配置。以後のクリックで次の間隔線を連続配置 |
| 移動・複写・全体移動・頂点編集 | 最終位置クリックで既に自動実行 | 現行維持 |
| 文字サイズ全体設定 | 値選択後に「新規の標準」「既存へ一括反映」 | 先に `反映先: 新規のみ／図面全体` を選び、プリセットまたは数値入力終了時に反映。適用ボタンは撤去 |
| 出力用紙・タイトル・台帳 | Applyなし、changeで更新 | 入力トランザクションだけ共通化し、ボタンは追加しない |
| 出力位置ドラッグ | モード開始後、pointerupで保存 | モード開始ボタンは残す。pointerupで1 Undoの現行維持 |

ボタンとして残すもの:

- 保存、名前を付けて保存、開く、新規、下絵を開く・差替。
- PNG、PDF、印刷、編集データ。外部処理を開始する実行ボタン。
- Undo、Redo、取消、1点戻す、選択解除。
- 削除、全消去、下絵削除、補助線消去。追加確認は出さず、Undoできることを条件に残す。
- 反転、位置を自動へ戻す、中央配置へ戻す、番号整理、リセット、隅切り復元、候補縮尺で収める。押すこと自体が編集内容の直接コマンドであり、別の確定を要求しない。
- 配置調整、面積表配置など、モードを開始するボタン。配置結果はキャンバス操作で自動保存する。

撤去するアクション名・孤立処理:

- `finish-command` の表示ボタン。
- `apply-calibration`、`apply-manual-scale`。
- `convert-shape-kind` の実行ボタン。
- `save-edit`、`cancel-edit` の孤立した旧処理。選択編集の確定UIとして復活させない。
- `save-typography-defaults`、`apply-typography-existing` の適用ボタン。反映先選択＋入力終了へ置き換える。

#### 22.9.3 共通入力トランザクション

現行は `input` で画面上のドラフトを更新し、`change` で文書へ保存している。`blur`を明示的に扱わず、IME終了時は最終値をドラフトへ同期するだけである。通常の `store.commit()` は文書全体スナップショットを毎回Undoへ積むため、1文字ごとのcommitへ変更してはいけない。

共通の入力処理を次の4操作へ集約する。

1. `beginInputTransaction`
   - focusin時に対象ページ、対象ID、フィールド、開始値、開始時dirty、関連フィールドを保存する。
2. `previewInput`
   - input中の生文字列を保持する。有効な値だけ編集ドラフトへ反映して描画するが、Undo履歴と保存文書は変更しない。
3. `flushPendingInput`
   - Enter、focusout、IME終了、選択変更、ページ変更、ワークスペース変更、ツール変更、保存、出力、新規、開く、終了前に呼ぶ。有効値を1回だけcommitする。
4. `cancelPendingInput`
   - 入力欄でEscを押した時、DOM・ドラフト・プレビューを開始値へ戻す。Undo、Redo、dirtyを変更しない。

離散操作:

- checkbox、select、色、プリセット、切替は操作時に1回だけcommitする。
- 面積入力に連動する坪、名称に連動させる幅員などは、関連する全変更を同じ1 Undoへ含める。
- 入力後に開始値へ戻した場合はcommitせず、dirtyと履歴を増やさない。

無効入力:

- 空欄途中、`-`、`.`、`1e`、範囲外、許可されない0・負数を生入力として保持し、文書へ保存しない。
- `aria-invalid` と欄横メッセージで理由を示し、ダイアログは使わない。
- 無効入力のまま保存・出力・ページ変更を要求された場合は遷移を安全に止め、その入力欄へフォーカスする。旧値へ黙って丸めない。

IME:

- composition中のinputはプレビュー専用にする。
- 変換候補を決めるEnterを作図終了として扱わない。
- compositionend直後のinput/changeとの二重commitを防ぎ、1 Undoだけ作る。
- 面積・坪の半角化と単位付加は、composition中ではなく最終入力終了時に行う。

遷移と未保存判定:

- 選択変更、PDFページ切替、作図ツール切替、出力画面への移動を各呼出側任せにせず、共通遷移入口で必ずflushする。
- `store.dirty || hasPendingInput` を未保存ありとして扱う。
- 保存・PDF・PNG・印刷は、出力データを作る前に共通flushを行う。
- 入力中のページ切替では、入力開始時に記録した元ページ・元対象へ保存し、切替先へ混入させない。

#### 22.9.4 Undoと破壊操作の安全条件

- 文字列1回、数値1回、select1回、種類変更1回、配置1回、ドラッグ1回を、それぞれCtrl+Z 1回で戻す。
- 1文字ごとに履歴を作らない。文書全体スナップショットの連続生成も避ける。
- 削除、全消去、種類変更、合筆、一括文字反映は確認なしで実行する代わりに、必ずUndo・Redoできる。
- 下絵削除は現状、描画ランタイムを先に破棄するため、文書Undoだけでは表示が復元しない可能性がある。確認なしへ統一する前に、Undo・Redo時の下絵ランタイム再生成を実装する。
- 新規、別ファイル読込、下絵の新規読込、終了は履歴自体を置換・破棄するため、未保存固定バーを残す。
- 即時編集後はステータス欄へ結果と `Ctrl+Zで戻せます` を表示する。ステータスは作業を妨げず自動消去する。

#### 22.9.5 横断実装順

この横断順序を22.7の個別UI順より先に適用する。

1. **入力基盤**
   - 共通入力トランザクション、IME、無効値、Esc復元、遷移前flush、未保存判定を実装する。
2. **固定メニューバー基盤**
   - 折返し可能な2～3段の固定レイアウト、通常色select、共通フィールド部品を作る。
3. **選択後編集**
   - 区画・道路・水路、全辺・個別辺、面積・坪、直接文字クリックを統合する。
4. **作成・加工・計測**
   - 全確定・配置ボタンを撤去し、各コマンドのクリック・ダブルクリック・Enter終了を統一する。
5. **縮尺・下絵**
   - 候補即時、任意入力終了、2点校正自動成立、縮尺未設定の固定案内、ページ別保存を実装する。
6. **注記・スタンプ・表・全体文字設定**
   - 残る動的ポップアップと適用ボタンを撤去し、同じ入力基盤へ接続する。
7. **出力・保存・破壊操作**
   - 遷移前flush、未保存バー、下絵Undo復元、外部出力の入力取り込みを確認する。
8. **全QA・実画面確認**
   - 自動QA、900×600～1920×1080、Windows倍率100～200%、Electron実操作を行う。

#### 22.9.6 必須回帰試験

- DOMと動的生成文字列に `details.toolbar-dropdown`、独自 `.color-picker-panel`、縮尺モーダル、`window.confirm`が残らない。
- 上部アプリメニューとOS標準画面以外、操作中に図面へ重なる浮動UIが出ない。
- Enter、focusout、IME終了、ツール切替、ページ切替、保存、終了の全経路で入力が失われない。
- 複数文字を入力してもUndoは1件だけ増え、1回のUndo／Redoで開始値・最終値を往復する。
- EscでDOM、プレビュー、文書、dirty、Redoが入力開始前へ完全に戻る。
- 全角数字、空欄途中、負号、小数点途中、範囲外を全数値欄で確認する。
- 区画・道路・水路・折れ線・面積・線・分割は確定ボタンなしでダブルクリック／Enter終了でき、二重点や次操作の幽霊点を残さない。
- 固定点数作図、スタンプ、文字、表、隅切り、等分、移動、複写は最終キャンバス操作で1回だけ作成される。
- 平行線は反転・間隔変更をプレビューでき、キャンバスクリックで連続配置できる。
- 種類変更、合筆、削除、全消去、一括反映、下絵削除を確認なしで実行し、Undo・Redoで完全復元する。
- 入力中のCtrl+S、PNG、PDF、印刷、新規、開く、終了で最新の有効値が欠落しない。
- PDFページ切替時、編集値と縮尺が元ページへ保存され、別ページへ混入しない。
- 900×600でも固定バーが図面へ重ならず、全項目へスクロールなし、または縦方向の自然な折返しだけで到達できる。

### 22.10 別角度の再検証: 実作業往復・データ境界・復旧・成果物

状態: **alpha.10対象のP0データ保護、保存・読込、出力実寸検査を実装済み。長期改善項目は設計記録として継続する。**

前節はUI部品と入力方式を中心に確認した。本節では、案件を開いてから保存・再読込・出力するまでの業務往復、複数の編集面が同じデータを触る時の競合、Undo後の画面復旧、実際のPDF/PNG成果物という別の境界から検証した。本節のP0ゲートを22.9のUI実装順より先に適用する。

#### 22.10.1 結論

- ポップアップと確定ボタンの撤去方針は維持する。
- ただし現状のままボタンだけを撤去すると、保存漏れ、案件間のdraft混入、全体スナップショットの過剰生成が増える。
- 現行QAが全PASSでも、分割後に重複辺IDができて編集データを保存できない実例を再現した。UIテスト合格とデータ健全性を分ける。
- 画面上で成功して見えることではなく、`作成／編集 → Undo → 保存 → 再読込 → 出力`の全往復が一致することを完了条件にする。

#### 22.10.2 P0: UI再編より先に直すデータ・案件境界

| P0 | 現状の危険 | 必須仕様 |
|---|---|---|
| 分割後の重複ID | `splitShape`、一括分割、折れ線分割が元の`edges`を両方の子へ渡す。再現では2子図形が`edge-2～5`を共有し、保存時の全体ID検査で拒否された | 分割計画段階で全子図形・全辺のIDを一意に割り当てる。外周辺の書式は幾何対応で複製し、切断辺は新ID。分割直後に`validateDocument`とserializeを必ず通す |
| 旧案件draftの新案件混入 | 作業ファイルを`store.replace`した後に`activateCommand`が旧draftをcommitし、同じIDの新案件図形を上書きし得る | 文書置換前に現在入力を保存／破棄のどちらかへ決着し、その後すべてのdraft・選択・サブ選択・配置状態を破棄する。文書置換後に旧draftをcommitしない |
| 台帳の見えない選択 | タブ・検索・種類絞込の外にある`registryIds`が残り、画面に見えない図形まで一括削除し得る。案件読込後も同じIDが残り得る | 絞込、タブ、ページ、案件を変えたら選択をクリアする。保持する場合は`非表示を含むN件`と全対象を固定表示し、見えない削除を禁止 |
| 台帳と上部編集の競合 | 台帳が保存文書を更新しても、古い上部draftがオブジェクト全体を後から置換し、先の価格・メモ変更を戻し得る | 保存は変更フィールドだけをpatchする。台帳と上部編集は同じ編集トランザクション／同じ最新revisionを参照し、古い全体コピーで置換しない |
| 台帳・出力入力の保存漏れ | 価格・メモ・タイトル・備考を入力中にCtrl+S／終了すると、change前のDOM値が保存されない | 全入力面を対象にした`flushActiveEditor`を保存、出力、ページ変更、新規、開く、終了の前に実行する |
| 下絵文書と表示ラスタの不一致 | 下絵削除、差替、PDFページ切替、画像90度回転をUndo/Redoしても、文書だけ戻り表示用runtime/sourceが戻らない | `source/currentPage/imageRotation`等の背景キーを履歴前後で比較し、Undo/Redo後に下絵runtimeを再生成する。非同期完了前は古いラスタを表示しない |
| 既存保存ファイルの破損 | 保存先へ直接writeし、書込み途中の異常で以前の正常ファイルを壊し得る | 同一フォルダの一時ファイルへ書込み、close/fsync相当後に置換する。失敗時は旧ファイルを保持し、dirtyを解除しない |

P0完了までは、確定ボタン撤去や種類select即時化を本体へ入れない。

#### 22.10.3 属性更新と不可逆操作

現行は、作成、読込正規化、1件編集、一括編集、種類変換が別々に属性を書き換えている。1件編集は1項目だけ変えても全フォーム値を書き戻し、一括編集は触った項目だけを書き戻すため、同じUIでも結果が異なる。

次の共通定義を設ける。

```text
PropertyDefinition
  role              // lot.name, road.width, metric.area, edge.dimension等
  supportedKinds
  scope             // object / selected objects / page / document default
  read(model)
  validate(raw)
  preview(draft)
  patch(model, value)
  createDefault
  mixedValue
  serializeVersion
```

- 1件・同種複数・作成時既定・台帳のすべてが同じ`PropertyDefinition`を通る。
- 保存文書へは触ったpropertyだけをpatchする。背景や非表示の値をフォーム既定値で上書きしない。
- 同種複数選択の異なる値は先頭値ではなく`複数値`と表示し、ユーザーが操作した項目だけ全件へ反映する。
- 区画⇔道路⇔水路は種類固有値を`kindStates`へ退避し、往復で復元する。これを採用する場合はschema v7にする。
- 家屋⇔駐車も、家屋だけの斜線等を退避して戻した時に復元する。
- 分割、合筆、種類変換について、番号、価格、メモ、任意面積文字、ラベル位置、辺表示、辺書式、道路幅員の採用元を一覧定義する。
- 破壊的変換は確認画面を出さず即時実行するが、1回のUndoで完全復元し、ステータスに採用元と戻し方を表示する。

#### 22.10.4 作業の往復と戻り先

| 作業 | 新しい規則 |
|---|---|
| 選択→分割・合筆・隅切り・頂点編集 | 現在選択がコマンド条件を満たす時は`targetIds`へ引継ぎ、同じ対象を再選択させない |
| 加工完了 | 既定は結果図形を選択して編集へ戻る。連続加工を残す場合は明示的な`連続`切替を固定バーへ出す |
| 加工取消 | 固定バーへ現在のEsc動作を表示し、加工開始前の選択を復元する |
| 水路の連続作図 | 1本確定後も直前の水路種別・名称・幅・色を保持し、次を道路へ戻さない |
| 作成設定の再利用 | `今回だけ／次回も使用`のスコープを表示し、特殊な1回設定を黙って文書既定へ保存しない |
| 道路・水路種類選択 | 既に手入力した名称を既定値で黙って上書きしない。自動変更する項目を固定バー上で明示する |
| 道路名・幅員の直接クリック | 対応する文字対象を開き、道路名または幅員入力へフォーカスする。文字移動だけで終わらせない |
| 面積・坪・辺寸法の直接クリック | 対応する文字対象／辺番号を開く。道路・水路でも同じ規則にする |
| 家屋・駐車寸法の直接クリック | 幅または奥行の対象を開く。rendererの`entity-dimension`を選択側で扱う |
| Ctrl／Shiftでラベル選択 | 本体クリックと同じ複数選択規則を適用する |
| 出力へ移動 | 入力をflush後、図面の選択、編集ページ、表示位置を一時退避する |
| 図面へ戻る | 出力前の選択、編集対象、表示位置を復元する。案件・ページが変わった場合だけ破棄する |
| 出力初期タブ | 校正または印刷縮尺が未設定なら`用紙`、準備済みなら前回タブを開く |

面積と坪、文字色と寸法色などの連動は、派生側が自動値の時または`連動`が明示ONの時だけ行う。手入力値を別項目の変更で黙って上書きしない。

#### 22.10.5 ページ、Undo、下絵

- PDFページを閲覧するだけの操作を通常の図形編集Undoへ積まない。最後に開いたページを保存したい場合は、履歴を増やさないnavigation stateとして扱う。
- 各履歴項目へ変更対象pageIdを記録する。別ページの編集をUndoした場合は、そのページの下絵runtimeを同期してから結果を表示する。
- ページ変更前に有効入力を元ページへflushする。未完作図は保存対象ではないため取消し、固定ステータスへ理由を表示する。
- ページ変更、新規案件、作業ファイル読込では、選択、個別辺、draft、marquee、outputDrag、`ui.output.placing`を全消去する。
- `document.calibration`はアクティブページの写しである。更新は必ずページ校正ヘルパーを通し、直接片側だけを書き換えない。
- 保存済みスナップショットの指紋を保持し、Undo／Redoで保存地点へ戻った時だけdirtyを解除する。

#### 22.10.6 出力成果物の物理検証

出力倍率の基本式 `mpp / printScale × dpi / 0.0254` は正しい。ただし、式が正しいことと成果物が正しいことを分ける。

- `printableRectMm`、収まり判定、中央配置、プレビュー、枠描画、PNG、PDF、印刷は1つの`PaperGeometryMm`を共有する。
- 現行は収まり判定の内側余白が約7mm、実際に描く枠が約4mm、A4のタイトル予約12mmに対して描画帯が約14.8mmであり、判定と見た目が一致しない。数値を1か所に統一する。
- PDF下絵は通常約302dpi相当で1回ラスタ化し、8千万画素上限で縮小される。小さい印刷分母へ拡大する時は、出力紙上の必要実効dpiから元PDFを再レンダーする。
- 現行PDFは300dpi Canvasを埋めたラスターPDFである。業務図面として当面使う場合も、線・文字・下絵の実効dpiを成果物から測る。将来のベクター出力経路をrendererから分離する。
- PNGはA4/A3の300dpi相当画素数だけでなく、pHYsへ11811 pixels/meterを明示する。外部ソフトで100%配置した時の物理寸法を確認する。
- 用紙枠OFFならタイトル帯も完全に消し、収まり判定からタイトル予約高さも除外する。
- タイトル、日付、作成者、備考は文書共通、用紙、印刷縮尺、配置、枠、下絵、ガイドはページ別であることをUIに表示する。

成果物検査はMediaBoxだけで終えない。100mの検尺線を1:500で出力し、PDF/印刷で200mm、300dpi PNGで約2362pxになることを実ファイルから測る。

#### 22.10.7 狭幅、高DPI、キーボード、IME

- 900×600、1366×768、1920×1080をWindows表示倍率100、125、150、175、200%で実際に切り替えて確認する。`devicePixelRatio`の記録だけを合格にしない。
- 固定2～3段は折返し可能にし、横方向の画面外配置を許さない。狭幅時は右一覧を固定領域内で折り畳み、出力設定は1列へする。
- 一覧の見出しを隠さず、価格・メモ・表示のラベルをプログラム上も視覚上も対応させる。19px入力・23pxボタンのような小さすぎる操作対象を改める。
- キャンバスを含む全フォーカス位置を見えるようにする。Escで入力からキャンバスへ移った時も現在位置が分かること。
- 編集ページ、一覧タブ、出力タブへ`tablist/tab/tabpanel`または同等の意味を持たせ、`aria-selected`、左右矢印、Home、Endを実装する。
- `#command-controls`全体をaria-liveにせず、変更結果専用の短いlive regionを設ける。入力フォーム全体を毎回読み上げさせない。
- 出力配置はマウスドラッグに加え、X/Y mm入力と矢印キー微調整を用意する。
- IMEはcompositionend後のinput/change二重発火でも1 Undoにし、変換Enterを作図終了へ流さない。

#### 22.10.8 仕様・バージョン・QAの整合

- 正本の優先順位は本書0章どおりとする。`V210_FULL_FEATURE_SPEC.md`と`V210_OPERATION_SPEC.md`はalpha.5時点で、確定ボタン、PDF縮尺候補、自動検出、schema v5等の古い記述を含む。実装開始前に各冒頭へ`最新判断はマスター計画を優先`と明示し、矛盾する期待値を更新する。
- package、画面、main、preload、core、io、保存JSON、QA、installerのバージョンを1つのビルド情報源から生成する。alpha.9画面でalpha.8 JSONを保存しない。
- DOM文字列の存在や内部APIへ直接値を入れる試験だけでは合格にしない。次の4層を必須にする。
  1. core不変条件: 全ID一意、トポロジー、属性継承、serialize。
  2. DOM実操作: pointer、Tab、Enter、blur、IME、Esc、Ctrl+S。
  3. 成果物: PDF MediaBox＋検尺線、PNG画素＋pHYs、下絵実効dpi。
  4. 実画面: 狭幅、高DPI、フォーカス、20回連続作業、installer起動。
- 296件旧QAのうち、`候補を選んでも確定ボタンまで適用しない`等の新仕様と逆の期待値を削除・置換する。

#### 22.10.9 改訂実装ゲート

1. **P0データ健全性**
   - 分割ID、案件置換順序、台帳非表示選択、draft競合を失敗する試験で固定し、修正する。
2. **P0保存・下絵復旧**
   - 原子的保存、保存地点dirty、下絵runtimeのUndo/Redo同期を実装する。
3. **共通入力トランザクション**
   - 全入力面のflush/cancel、IME、無効値、field patchを実装する。
4. **属性定義とschema**
   - PropertyDefinition、混在値、種類固有退避、v6→v7移行の要否を確定する。
5. **作業遷移**
   - 選択引継ぎ、直接文字クリック、連続水路、出力往復、囲い選択を整える。
6. **固定メニューバーUI**
   - ここで初めてポップアップ、確定・適用・配置ボタンを撤去する。
7. **出力物理・アクセシビリティ**
   - PaperGeometryMm、下絵再レンダー、PNG密度、狭幅・高DPI・キーボードを完成させる。
8. **移行・成果物・実機QA**
   - v3/v4/v5/v6固定fixture、全ID検査、実PDF/PNG検尺、installerスモークを通す。

#### 22.10.10 追加の必須回帰シナリオ

- 区画・道路・水路を直線／折れ線／一括で分割し、全IDが一意で保存・再読込できる。
- A案件で1件／複数編集の入力途中にB案件を開き、Bの同一ID図形が変化しない。
- 台帳の別タブ・検索外に選択を残しても、見えない対象を削除できない。
- 台帳でメモ変更→上部で色変更、および逆順でも両方が残る。
- 台帳、タイトル、備考へ入力直後にCtrl+S／PDF／終了し、最新値が保存される。
- 下絵削除、差替、ページ切替、90度回転をUndo/Redoし、文書と表示ラスタが一致する。
- 水路を3本連続作図し、すべてwater・同じ水路既定になる。
- lot→road→water→lotで、各種類の番号、価格、名称、幅員、位置、辺書式が仕様どおり復元する。
- 同種3件の異なる値を選び、文字サイズだけ変えて他項目が変わらない。
- 道路名、幅員、面積、坪、辺寸法、家屋寸法を直接クリックし、対応入力へ到達する。
- 選択済み図形から分割・合筆・隅切り・頂点編集へ入り、対象を再選択せず完了できる。
- 出力確認から図面へ戻り、直前の選択、編集ページ、表示位置が復元する。
- 2ページPDFへ別縮尺・別出力設定を持たせ、往復、Undo、保存、再読込、成果物が一致する。
- 100m検尺線のPDF/PNG実測、A4/A3縦横、枠ON/OFF、下絵・ガイドON/OFF、低実効dpi警告を確認する。
- Tab順、フォーカス表示、IME、Esc、狭幅、高DPI、20回連続作業で二重作成・幽霊点・入力欠落がない。
- packageから保存JSONまで全箇所が同一バージョンで、v3/v4/v5/v6を移行後も保存できる。

---

## 23. 関連資料

- V210_FULL_FEATURE_SPEC.md: v2.1の全機能再設計仕様
- ui-usability-review-v210.md: 実操作で確認したUI問題
- V210_OPERATION_SPEC.md: 操作仕様
- qa-v210.cjs: 自動QA
- ユーザー提供の各スクリーンショット: UI・表示・出力の具体例

上記資料は過去判断と詳細根拠として保持する。ただし、確定ボタン、PDF縮尺、自動検出、schema、実装順などで本書と矛盾する箇所は、本書の最新判断を正本として読み替える。
