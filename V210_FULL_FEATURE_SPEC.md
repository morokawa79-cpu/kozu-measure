# 土地区画作成工房 v2.1 全機能再設計仕様

対象: `v2.1.0-alpha.5`
方針: 旧コードは実行・移植せず、挙動の棚卸し資料としてだけ参照する。

## 1. 製品コンセプト

Jw_cad系の「画面を隠さず、同じ操作を繰り返せる」考え方を、公図・地図画像の読込と区画作成へ絞って再構成する。

- 初めて使う人: 左の7カテゴリから日本語コマンドを選び、上段に出る次操作だけを順に行う。
- 慣れた人: キーで同じコマンドを直接開始する。
- どちらも同じコマンド状態機械を使う。初心者用画面と熟練者用処理を二重実装しない。
- 図面上へHTMLパネル、モーダル、追従ウィンドウを出さない。
- コマンドを替えたら、未確定点・対象・一時入力・プレビューを必ず破棄する。文書設定や選択とは混ぜない。
- 一覧と出力は、図面と同じ中央領域を切り替える。左右から図面を覆わず、図面のズーム・位置も変えない。
- 自動Fitはしない。「全体」操作を実行したときだけ表示倍率を変える。

## 2. 画面構成

| 領域 | 役割 |
|---|---|
| 上1段 30px | 文書名、バージョン、保存状態、ファイル操作 |
| 上2段 46px | 選択中カテゴリのコマンド、または実行中コマンドの入力と確定 |
| 左54px | 下絵、選択、区画、道路、加工、計測、注記の7カテゴリ |
| 中央 | 図面／台帳／出力の排他的ワークスペース |
| 右54px | Undo、Redo、保存、全体、100%、表示、台帳、出力 |
| 下27px | 現在コマンド、次の操作、座標、縮尺、ズーム、吸着状態 |

最小外寸は900×600。上部は常に2段固定で、折返し・横スクロール・3段化をしない。

## 3. 共通操作規則

| 操作 | 全作図コマンドでの意味 |
|---|---|
| 左クリック | 任意点を指定 |
| 右クリック | 既存頂点・交点・辺を読み取って指定 |
| Enter | 条件を満たした図形を確定 |
| Backspace | 作図中の点を1点だけ戻す |
| Esc | 現在コマンドの未確定状態を取消 |
| Delete | 選択済みオブジェクトを削除 |
| ホイール | カーソル位置を中心にズーム |
| 中ボタンドラッグ／Space+ドラッグ | パン |
| Ctrl+Z／Ctrl+Y | 文書変更をUndo／Redo |

文字入力のIME変換中はEnter、Esc、ショートカットを処理しない。

## 4. 状態の分離

```text
DocumentStore
  background / calibration / paper / pages / shapes / entities / preferences / nextId

ViewState
  x / y / zoom

SelectionState
  selected IDs / active part / active vertex or edge

CommandSession
  category / command / step / draft points / targets / transient form / preview

Runtime
  decoded PDF/image / pointer drag / canvas DPR / render cache
```

- 図面／一覧／出力のワークスペースを切り替えた時点で、未確定の点・対象・入力値・プレビューを破棄する。戻ったときは選択ランチャーから再開し、直前の作図コマンドを表示しない。

- Undo/RedoはDocumentStoreの完全スナップショットを対象にし、ID、背景、縮尺、用紙、全ページを含める。
- ViewState変更は図面座標を変えず、通常のUndo対象にしない。
- 別プロジェクト読込時はfresh documentを生成後に一括置換し、履歴、選択、コマンド、runtimeを全消去する。
- 旧`localStorage["kozu.shapePreferences.v1"]`は読まない。
- 色等の再利用は新しい文書内preferencesだけ。区画、道路、水路、文字、線、スタンプを別々に保持する。

## 5. データモデル v5

- 1プロジェクトに複数ページを持ち、各ページが独立した`shapes`と`entities`を持つ。
- `shape.kind`: `lot | road | water | cutout`
- `entity.kind`: `distance | polyline | area | line | arrow | text | callout | north | house | parking | lot-table | parallel | guide | dimension`
- 座標は常に図面world座標。縮尺変更は`calibration.mpp`だけを変更し、頂点や文字位置を拡縮しない。
- 区画価格は数値（万円）で保存する。文字列から数値を推測しない。
- 辺は独立IDを持ち、非表示、個別文字、色、倍率、書体、角度、位置を保持する。
- 面積表は既定で区画へ動的リンクする。固定化するときだけsnapshotを保存する。

## 6. 全機能棚卸しとv2.1での扱い

### 下絵・縮尺・表示

| 旧機能 | v2.1仕様 |
|---|---|
| PDF読込 | 「新規で開く」と「下絵だけ差替」を分ける。新規はfresh document、差替は図形保持 |
| 画像読込／貼付 | PNG/JPEG/WebP、D&D、クリップボードに対応。置換前に用途を明示 |
| PDFページ送り | ページごとの図形を保存し、往復・保存・再読込しても消さない |
| 画像90度回転 | 0/90/180/270を文書へ保存。新規背景では0 |
| 下絵位置調整 | X、Y、倍率、自由角度、不透明度、表示、ロックを単一モデルで管理 |
| 2点キャリブレーション | 2点＋実距離。IME対応の通常inputで入力。座標は変更しない |
| 手動縮尺 | 1/100等候補または数値。候補選択後に確定して適用 |
| PDF縮尺検出 | 自動適用せず候補として提示 |
| ズーム／パン／全体／100% | どのコマンド中も同じ操作。コマンド切替やresizeで自動Fitしない |
| 吸着 | 頂点、交点、辺、グリッドを独立ON/OFF。右クリック読取と状態表示 |

### 区画・道路・選択

| 機能 | v2.1仕様 |
|---|---|
| 区画作図 | 3点以上。重複点・自己交差・面積0は確定不可。黄色系を初期色にする |
| 道路作図 | 区画と同じ自由ポリゴン。道路種別、名称、幅員属性、縦書き、灰色系style |
| 水路 | 道路基盤を共用し、種別と水色系styleを明示 |
| 道路名称 | 道路／公道／私道／位置指定道路／認定外道路／水路の候補＋自由入力 |
| 区画属性 | 番号、上部ラベル、価格、メモ、塗り、線、透明度、番号／面積／坪／寸法表示 |
| 個別移動 | 選択対象だけ移動。空白ドラッグで全体移動しない |
| 全体移動 | 専用コマンドで図形のみ／下絵込みを明示 |
| 頂点編集 | ドラッグ中preview、確定前にトポロジー検査。自己交差は却下 |
| 文字・寸法位置 | ラベルと各辺寸法を個別に移動、保存、Undo |
| 個別文字書式 | 書体、色、大きさ、角度、縦書き、背景、枠、非表示 |
| 面積・坪の個別文字 | 面積と坪を独立して、本文（空欄は自動計算）、書体、色、大きさ、角度、縦書き、表示、位置を編集 |
| 個別寸法書式 | 内容、約表記、桁数、丸め、補正、色、大きさ、角度、位置、非表示 |
| コピー／貼付 | 新ID。区画は新番号。属性と価格を複製する旨を表示 |
| 削除 | Deleteは選択削除、Backspaceは作図点を1点戻す用途に固定 |
| 番号整理 | 区画だけを連番化。道路・水路・隅切りを除外 |

### 区画加工

| 機能 | v2.1仕様 |
|---|---|
| 選択分割 | lotのみ。2点以上の折れ線で2polygonへ分割。価格は勝手に複製せず未設定にする |
| 一括分割 | 2点以上の折れ線と交差するlotを全件事前計算し、全件正常な場合だけ1回でcommit |
| 合筆 | 隣接lot同士のみ。共有辺を除去。価格は未設定または明示的な合算 |
| 隅切り | 隣接辺内の距離だけ許可。元lotと`cutout`を生成し面積分類を分ける。縮尺未設定では確定しない |
| 2点間均等ガイド | 任意の2点間を2〜20等分し、等分点・1区間距離・全長を表示。区画は分割しない。保存・Undo対象 |
| 区画等分線 | 選択区画を基準方向に2〜20等分する平行補助線群。2点間均等ガイドとは別コマンド |
| 平行補助線 | 2点の基準線＋実距離＋左右。同じ基準線を保持し、d、2d、3d…を連続配置。縮尺未設定では確定しない |
| 補助線全消去 | 均等・区画等分・平行補助線だけを一括削除。Undo可能 |
| 辺寸法非表示 | 辺IDへ記録し、作図変更時は幾何対応で再割当 |

### 計測・注記・スタンプ

| 機能 | v2.1仕様 |
|---|---|
| 距離 | 2点。実距離、表示、一覧、出力を同一計算関数から生成 |
| 折れ線距離 | 合計と区間値を分離。個別文字を編集しても派生値を失わない |
| 面積 | ㎡と坪。坪換算は全箇所で`1坪=3.3057851239669422㎡` |
| 直線 | 実線／破線／点線、太さ、色。正式コマンドとして到達可能にする |
| 矢印 | 線、矢尻、色、太さ |
| 文字 | 書体、色、大きさ、角度、縦書き、背景、枠／下線／なし |
| 引出線 | 先端、折点、文字位置を個別編集 |
| 北マーク | 角度、倍率、色。配置前previewと連続配置 |
| 家屋 | 幅、奥行、角度、ラベル、線種、色、寸法ON/OFF。配置前preview |
| 駐車場 | 幅、奥行、角度、ラベル、線種、色、寸法ON/OFF。配置前preview |
| 配置済みスタンプ種別 | 家屋と駐車場を削除・再配置せず相互変更 |
| 面積表 | 区画番号、㎡、坪、価格、合計。既定は動的リンク、固定化可 |

### 台帳・用紙・出力

| 機能 | v2.1仕様 |
|---|---|
| 統合台帳 | 区画道路、計測、注記スタンプ、表ガイドを1画面で検索・選択・編集・削除 |
| 区画価格 | 万円の数値入力、区画表示、一覧合計、面積表へ反映 |
| 白紙 | A4/A3、縦横、下絵なしで開始。切替で既存座標を変更しない |
| タイトル枠 | 図面名、日付、作成者、縮尺。長い日本語を収める |
| PNG | 成果物専用renderer。選択、頂点、draft、previewを含めない |
| 印刷／PDF | 同じ成果物rendererから生成し、全shape/entityと背景を含める |
| 編集JSON | schema v5検証、全ページ保存、旧v3 migration、NaN・重複ID・破損点を拒否 |
| 終了確認 | dirty時だけ保存／破棄／取消。保存失敗時は閉じない |

## 7. 明示的に搭載しないもの

- 道路セットバック: 旧版には道路名の文字列と未接続checkboxしかなく、ユーザー指示により削除。
- 買取価格試算／収支積算: 旧版には未接続UIしかなく、ユーザー指示によりUI・処理・保存データを削除。
- 旧直接PDF生成関数: 区画・日本語・スタンプを扱えない孤立実装なので移植しない。統一成果物rendererから印刷／PDF出力する。
- 旧DOM、hidden form、proxy click、`app.js`、`next-ui.*`、`ui-v140-alpha1.*`、`jww-v200.js`の実行またはimport。

## 8. 旧JSON移行

- 旧`lots`は`shape`へ正規化し、lotとroadの混在fieldを除去する。
- `items`は計測、線、矢印、平行guideへ移行する。
- `texts`は文字、引出線、北、家屋、駐車場、面積表へ移行する。
- `divGuides`が存在するときはguideへ移行する。
- 背景、回転、縮尺、用紙は値が存在するときだけ旧入力から採用する。ただし前セッション値は一切参照しない。
- 未接続だった買取・セットバックfield、未知field、非有限座標、壊れたpolygonは移行しない。警告件数を返す。
- 読込成功時にDocumentStoreを丸ごと置換し、Undo/Redo、選択、CommandSession、runtimeを初期化する。

## 9. 総合受入条件

1. 旧localStorageが残っていても最初の区画は黄色、道路は灰色で始まる。
2. project Aの後に背景なし・縮尺なしproject Bを読むと、Aの背景・縮尺・履歴が残らない。
3. 縮尺変更前後で全頂点・文字・スタンプ座標が完全一致する。
4. 複数ページPDFの各ページに作図し、往復・保存・再読込後も保持される。
5. 全作成コマンドに、作成→編集→Undo→Redo→保存→読込→成果物出力の経路がある。
6. 不正polygonを作る分割・一括分割・合筆・隅切りは文書を一切変更しない。
7. コマンド切替後、前コマンドの点、target、form、previewが0件になる。
8. IME変換中のEnterで作図確定やショートカットが発火しない。
9. 884×535のrenderer領域でも、上2段、左右rail、下statusが欠けず、図面上へHTMLが重ならない。
10. PNG／印刷／PDFに背景、全shape、全entity、用紙枠が入り、選択表示とpreviewは入らない。
11. 3点以上の折れ線で選択分割・一括分割が成立し、曖昧な多重交差では全体を変更しない。
12. 任意2点均等ガイドと区画等分線は別コマンドで、前者は等分点・区間距離・全長を表示する。
13. 平行線は同じ基準線のままd・2dを連続作成でき、隅切りとともに縮尺未設定では確定しない。
14. 面積と坪の文字は独立して編集・ドラッグでき、保存再読込・複写・分割後も保持される。
15. 動的／固定の両面積表に、表示中区画の㎡・坪・価格の合計行が入る。
16. 配置済み家屋／駐車場は種別変更でき、補助線全消去はUndoで戻せる。
