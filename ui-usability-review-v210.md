# v2.1.0 UI使いやすさレビュー（実装はSonnet担当）

2026-07-16 Fableによる実操作調査。ブラウザ上で実フロー（下絵→縮尺→作図→編集→出力）を
操作して確認済みのもののみ記載。優先度順。

---

## 高優先度（作業のたびに引っかかる）

### 1. 縮尺合わせ：2点クリック後にフォーカスが実距離欄へ移らない
- **現象**: 図面上で2点をクリックした後、フォーカスがキャンバスに残る（実測で確認）。
  ユーザーは毎回マウスをツールバーまで運び、実距離欄をクリックしてから入力する必要がある。
- **改善案**: `handleCanvasPoint` の calibrate 分岐で2点目を取得した直後に
  `$('[data-field="calibration-distance"]', dom.commandControls)?.focus()` を呼ぶ。
  Enterで縮尺確定は既に実装済み（keydown handler）なので、これだけで
  「2点クリック→数字入力→Enter」の3操作で完結するようになる。
- **場所**: v210/app.js の `handleCanvasPoint` calibrate分岐（2185行付近）

### 2. 文字（注記）の内容編集が「台帳情報▾」の中に隠れている
- **現象**: 文字エンティティを選択すると、編集パネルには「文字書式▾」「台帳情報▾」の
  2つのドロップダウンしか出ない。**肝心の文字内容**は「台帳情報▾」を開いた中の
  「名称」欄にあり、名前も場所も直感に反する（実測で確認）。
- **改善案**: 図形以外（text/callout等、`hasEditableText`かつ`!isShape`）のときは
  `object-label` 入力を「文字」というラベルでドロップダウンの外（行の先頭）に直接出す。
  区画・道路のときは現状どおり台帳情報の中でよい。
- **場所**: index-v210.html `controls-object-basic` テンプレート + v210/app.js
  `showObjectEditor` の表示切替（isShape分岐）

### 3. 複数選択（一括編集）のタブが統合前の旧構成のまま
- **現象**: 単一選択は「基本／面積・坪／寸法」だが、複数選択にすると
  「表示／文字／寸法」という統合前のタブ構成に切り替わる（実測で確認）。
  同じ操作体系のはずの画面でタブが変わり、ユーザーの学習を裏切る。
- **改善案**: `showBatchEditor` の `allowedPages` を単一選択と同じく
  `object-basic` 基準に統一し、一括編集でも統合済み「基本」タブ
  （塗り/外枠/濃さ/表示項目/文字書式）を出す。一括で変更できない項目
  （番号・名称・価格・メモ＝台帳情報）は既存の「変更した項目だけ反映」注記どおり
  非表示のままでよい。
- **場所**: v210/app.js `showBatchEditor`（797行付近）

## 中優先度

### 4. 出力ワークスペースの初期タブが「用紙」
- **現象**: 「出力」を開く目的はほぼ書き出し（PDF/印刷/PNG）だが、初期表示タブは
  「用紙」。毎回「書き出し」タブへの1クリックが挟まる。用紙設定は最初に1回
  設定したら変えないことが多い。
- **改善案**: 出力ワークスペースの初期タブを「書き出し」にする。
  （または用紙未設定の初回のみ「用紙」を出す）
- **場所**: v210/app.js 出力ワークスペースのタブ初期値

### 5. Ctrl+P（印刷）ショートカットがない
- **現象**: 印刷は 出力レール→書き出しタブ→印刷カード の3クリック。
  keydownハンドラは Ctrl+S/O/N/Z/Y/C/V のみ対応。
- **改善案**: Ctrl+P で印刷ダイアログ（printOrPdf の print 経路）を直接起動。
- **場所**: v210/app.js `handleKeyDown`（4150行付近）

### 6. sheet-button（一覧・出力画面のボタン）のホバーで白文字が消える
- **現象**: `.sheet-button:hover` が背景を白にするが、`.sheet-button.primary` /
  `.sheet-button.danger`（例: 一覧の赤い「選択を削除」）は白文字のため、
  ホバー中に文字が背景に溶けて読めなくなる。ctrl-btn系は修正済みだが
  sheet-button系が未修正（CSS詳細度の同型バグ）。
- **改善案**: 修正済みの ctrl-btn と同じパターンを追加:
  ```css
  .sheet-button.primary:hover:not(:disabled) { color:#fff; background:#1a68b8; border-color:var(--active-dark); }
  .sheet-button.danger:hover:not(:disabled) { color:#fff; background:#b82b2b; border-color:var(--danger-dark); }
  ```
- **場所**: jww-v210.css 1029行付近（`.sheet-button:hover` の直後）

### 7. 空キャンバスにドロップ誘導がない
- **現象**: 起動直後のキャンバスは無地のグレー。PDF/画像のドラッグ&ドロップに
  対応しているが、その手がかりはステータスバーとツールバーの小さな文字のみ。
- **改善案**: 下絵未読込かつ図形0のとき、キャンバス中央に薄い案内
  （「ここにPDF・画像をドロップ　または［下絵を開く］」）を描画する。
  renderer のオーバーレイでもDOMオーバーレイでも可。ドラッグ進入時に
  ハイライトするとなお良い。
- **場所**: v210/app.js render/makeOverlay まわり + ドロップハンドラ（4210行付近）

## 低優先度（余裕があれば）

### 8. 選択した図形をそのままドラッグで移動できない
- 選択モードで図形本体をドラッグしても動かない（移動は「移動」ボタン→基準点→
  移動先の3クリック方式のみ）。文字ラベルは直接ドラッグ可能にしたので、
  図形本体も「選択中の図形をドラッグ＝移動」にすると一貫する。
  ※スナップ・履歴との絡みがあるので設計判断が必要。誤動作（クリックのつもりが
  微小ドラッグ）対策に数px のドラッグ開始しきい値を設けること。

### 9. 頂点も選択中に直接ドラッグできない
- 頂点編集は専用コマンド（Z）のみ。選択中に頂点ハンドルを表示している
  のだから、そのままつかんで動かせると自然。8と同様の設計判断が必要。

### 10. 「頂点」ボタンのラベルが素っ気ない
- 基本タブ先頭の「頂点」ボタンは「頂点を編集」の方が分かりやすい。
  幅に余裕がなければ title 属性の補強でも可。

### 11. 文字コマンドの既定文字列が「文字」のまま配置される
- 文字ツールで内容を入力し忘れて配置すると「文字」という文字列が置かれる。
  配置後の編集導線が2の修正で改善されれば実害は減るが、配置直後に
  内容入力欄へフォーカスを移す等の後押しがあるとなお良い。

---

# 追加調査（第2回）

## 最重要（データ消失の罠）

### 12. 未保存の作業が確認なしに消える（Undoも不能）
- **現象（ライブ実証済み）**: 図形を描いた未保存状態で「下絵を開く」から新しい
  ファイルを選ぶと、**確認ダイアログなしに全図形が消え、履歴もクリアされるため
  Undoでも戻せない**。同じ問題が以下の3経路すべてにある:
  - 「下絵を開く」（Ctrl+O / 青いprimaryボタン / メニュー）→ `loadUnderlay(file, false)` が
    文書を作り直し `store.clearHistory()` まで実行
  - 「新規作成」（Ctrl+N / メニュー）→ `store.replace(K.createDocument(), {clean:true})`
  - 「作業ファイルを開く」→ `store.replace(loaded.document, {clean:true})`
- アプリを**閉じる時だけ**は「保存/破棄/キャンセル」の確認がある
  （app-close-requested フロー）。同じ保護をこの3経路に広げるだけで防げる。
- **改善案**: `store.dirty` かつ図形が1つ以上あるとき、3経路の実行前に
  閉じる時と同様の確認（保存して続行/破棄して続行/キャンセル）を挟む。
  実装は `loadUnderlay`(3062行) / `new-project`(3682行) / `openProjectFile`(3146行) の
  冒頭にガード関数を共通で入れるのが簡潔。
- **場所**: v210/app.js 上記3箇所 + 確認UI（closeフローの流用可）

### 13. 下絵読込後も「下絵を開く」が青いprimaryのまま
- **現象**: 下絵を読み込んだ後もツールバー先頭の「下絵を開く」が青の最優先
  ボタンのまま。読込後の主作業は縮尺合わせなのに、押すと12の全消去が起きる
  ボタンが一番目立っている（安全な「差替」は地味な通常ボタン）。
- **改善案**: 下絵読込済みのときは「下絵を開く」を通常ボタンにし、
  primaryを縮尺側（「縮尺を確定」）へ移す。12の確認ダイアログと併せて二重の防御になる。
- **場所**: v210/app.js `addDynamicCommandControls` の calibrate/underlay-open 分岐で
  `background.type` の有無に応じて primary クラスを付け替え

## 文言・表記の不整合

### 14. 「白紙から開始」は白紙にならない
- **現象（実証済み）**: メニューの「白紙から開始」を実行しても描いた図形は
  すべて残る（実際の動作は「下絵なしの用紙に切り替える」）。
  「開始」という言葉が新規作成を連想させ、12の危険操作と混同しやすい。
- **改善案**: 「下絵なしで作図」「白紙の用紙にする」等、動作どおりの名前に変更。
  （ファイルメニュー内の位置も「新規作成」の隣なので誤解を助長している）
- **場所**: index-v210.html 76行（メニュー）・171行（ツールバー「白紙」）

### 15. 同じ画面に3つの名前
- **現象**: 右レールは「検索」、表示メニューは「一覧検索へ」、開いたパネルの
  見出しは「一覧・台帳」— すべて同じ一覧パネル。
- **改善案**: いずれかに統一（パネル見出しの「一覧・台帳」を正とし、
  レール・メニューも「一覧」または「台帳」に揃えるのが簡潔）。
- **場所**: index-v210.html 141行・308行

### 16. ヘルプで「全体」が2つの意味
- **現象**: ショートカットヘルプ基本ページに「⇧X 全体(移動)」と「F 全体(表示)」が
  並び、どちらも「全体」表記で紛らわしい。
- **改善案**: 「全体移動」「全体表示」とフル表記にする。
- **場所**: index-v210.html のヘルプグリッド定義

## 低優先度

### 17. 縮尺未設定のまま区画を描いたときの説明不足
- 縮尺未設定でも区画は描けるが（これ自体は後から縮尺を設定すれば数値が入るので
  正しい仕様）、作成直後のステータスは「区画を作成しました」だけで、寸法・面積が
  表示されない理由を言わない。「縮尺未設定のため寸法は表示されません。
  ステータスバーの『縮尺 未設定』から設定できます」と一言添えると迷わない。

### 18. ステータスバーのズーム表示だけクリック不可
- 「縮尺 1:378」「自動吸着 ON」はボタンなのに、隣の「100%」(ズーム)だけ
  ただの表示。同じ見た目で片方だけ反応しないのは微妙に混乱する。
  クリックで100%へ戻す（actual-size）を割り当てると自然。

---

## 検証環境メモ
- QAハーネス: `node qa-v210.cjs`（現在258項目、全PASS状態から着手すること）
- 変更後は必ず QA 全通過 + `node_modules/electron/dist/electron.exe .` での
  起動スモーク（JSエラーなし）を確認。
- ツールバーは2段固定・ポップアップは overflow:visible 前提。行内に項目を
  足すときは 900px 最小幅での横はみ出しに注意（QAの compact-* チェックが検出する）。
